apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: eppne
resources:
- namespace.yaml
- configmap.yaml
- secret.yaml
- external-secret.yaml
- deployment.yaml
- service.yaml
- hpa.yaml
- network-policy.yaml
# 🔥 إضافة patches إذا لزم الأمر (مثل تغيير بيئة التطوير)
# patches:
# - path: patches/development.yaml
#   target:
#     kind: ConfigMap
#     name: eppne-config