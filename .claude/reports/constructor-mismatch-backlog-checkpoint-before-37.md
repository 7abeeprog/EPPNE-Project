# نقطة تفتيش — قبل البدء في #37 (الإصلاح المالي)

**تاريخ:** 2026-08-28
**الحالة:** توقف مؤقت بانتظار تأكيدك — صفر تنفيذ على #37 حتى الآن.

---

## ✅ اللي خلص فعليًا لحد الآن (مُتحقَّق منه حيًا، كل التحديثات في `constructor-mismatch-backlog-classification.md`)

### 1. الـ9 بنود القديمة (بند 1 من قائمة القرارات)
#1، #5، #6، #7، #8، #9، #11 (بشقيه #11a/#11b)، #14، #15 — كلها اتعلَّمت ✅ في الملف الموحَّد،
كل واحد مصحوب باقتباس حي مباشر من تقرير الجلسة الأصلي بتاعه (مش من `PROGRESS_LOG.md` كوسيط
ومش من رسالة commit). أمثلة على الأدلة المقتبَسة:
- #1: *"15 اختبارًا ... 15 passed × تشغيلتين متتاليتين، صفر تذبذب"*
- #5: *"`GET /api/sovereign-entities/ → 401`"* (بدل 500 بلا حاجز مصادقة)
- #9: *"الرقم الحاسم: 4 من 4 دوال أثبتت حيًا إن `_check_saas_limits` ... بقت شغالة فعليًا بلا استثناء — 100%"*

### 2. #28 (`saas-service-catalog-missing-entries`) — بند 5 من قائمة القرارات
نُفِّذ فورًا كما طلبت (بيانات seed فقط، صفر كود). 4 صفوف اتضافت في `saas_service_catalog`
(`tenders` id=74, `auctions` id=75, `service_marketplace` id=76, `zamakana` id=77).

**تحقق حي:** `SaaSRepository.get_service_by_code()` رجعت صفوف حقيقية للأربعة (كانت بترجع `None`
قبل كده). `can_access_service()` بترجع `False` لسه لتينانت1 — متوقَّع وصحيح (بلا اشتراك فعلي)، مش
نفس سبب الفشل القديم.

**⚠️ اكتشاف جانبي أثناء التحقق:** الكتالوج كان شبه فاضي تمامًا (3 صفوف بس قبل الإصلاح، اتنين
منهم بادئة `TEST_REQSECTOR_SESSION_*` — بيانات throwaway مش seed حقيقي). و**`transport`/
`tourism_sports`/`social` بتستخدم نفس آلية `can_access_service(code)` بأكواد لسه ناقصة من
الكتالوج**، بنفس الأثر بالضبط — لكنها مش من ضمن الـ3-4 دومينات الموصوفة أصلًا في #28، وغير
مُصلَحة. (ملاحظة: commit `6b4703a` بتاع #31 أكَّد نفس الاكتشاف بشكل مستقل — تطابق جيد.)

### 3. #31 (`frontend-service-url-prefix-mismatch`) — بند 3 من قائمة القرارات
راجعت commit `6b4703a` واستخرجت دليل حي فعلي (مش رسالة الـcommit وحدها) من
`frontend-url-prefix-comprehensive-audit-session-log.md` §12 — جدول طلب حقيقي واحد لكل ملف من
الـ21 ضد سيرفر حي، بعد التصحيح: *"10/21 نجح بالكامل ... 9/21 وصل للراوتر الصحيح لكن كشف أعطال
backend معروفة مسبقًا ... 2/21 كشفوا أعطال backend جديدة"* + `tsc --noEmit` قبل/بعد أظهر
byte-identical output. اتعلَّم ✅ في الملف الموحَّد (الصف + القسم المخصَّص للبند).

---

## ⚠️ نقطة قبل ما أبدأ #37 (`finance-transfer-payment-tx-hash-broken`)

البند ده — تخزين كائن `Transaction` كامل بدل `.tx_hash` نصي — مؤكَّد في 4 مواضع عبر 3-4 دومينات:
`realestate`، `insurance`، `transport.pay_delivery`، `tourism_sports.book_program`.

**المشكلة:** `transport` (واحدة من الأربعة) **جداولها مش موجودة في قاعدة البيانات أصلًا** — نفس
القيد البنيوي الموثَّق سابقًا في #18/#39 (`transport-domain-tables-never-migrated`). يعني:

- **3 مواضع** (`realestate`, `insurance`, `tourism_sports`) — أقدر أعمل التحقق الحي الكامل اللي
  طلبته (كل موضع + تحقق DB حقيقي بعده، مش بس `tsc`).
- **موضع `transport.pay_delivery`** — هياخد نفس معاملة #18 (فحص سورس كود فقط: تأكيد إن `.tx_hash`
  اتستخرجت صح في الكود، بلا إمكانية تشغيل السيناريو فعليًا لعدم وجود الجداول) — هعلّمه بنفس
  التحفظ الصريح، مش هدّعي تحقق حي كامل عليه.

**السؤال:** أكمل على #37 دلوقتي بنفس المنطق ده (3 مواضع تحقق حي كامل + موضع واحد فحص كود بتحفظ
موثَّق)؟
