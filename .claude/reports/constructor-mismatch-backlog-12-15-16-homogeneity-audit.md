# تدقيق تقني: هل إصلاح Backlog #12 و#15 و#16 متطابق فعلاً؟ (تصحيح لتصنيف "دفعة واحدة")

**تاريخ الإعداد:** 2026-08-17
**الغرض:** الإجابة على السؤال: "بالنسبة للدفعتين (يقصد التجميع المقترَح لـ#12+#15+#16 في ملف `constructor-mismatch-backlog-classification.md`) هل فعلاً الإصلاح المطلوب لكل واحد منهم متطابق تقنيًا (نفس نوع التعديل: نقل `tenant_id` من الاستدعاء للـconstructor)، ولا كل واحد فيهم شكل مختلف شوية (بعضهم 'باج مركّب' زي `arbitration_syndicates` وبعضهم 'بسيط')؟"
**المنهجية:** `grep` شامل + قراءة مباشرة لكل موضع استدعاء حقيقي للثلاث دوال (`can_access_service`, `check_and_consume`, `execute_agent_action`) عبر كل دومينات المشروع، ومقارنة كل استدعاء بالتوقيع الحقيقي الفعلي للدالة (مش افتراض).

**النتيجة المختصرة: لا، مش متطابق. الادعاء السابق في `constructor-mismatch-backlog-classification.md` (إن #12/#15/#16 "نفس السبب الجذري، نفس الإصلاح لكل الدومينات") غير دقيق لـ#15 و#16 تحديدًا — تصحيح رسمي أسفل.**

---

## التوقيعات الحقيقية (مصدر الحقيقة)

```python
# saas/service.py:209
async def can_access_service(self, service_code: str) -> bool

# ai_governance/service.py:140-149
async def check_and_consume(
    self, agent_id: int, user_id: int, action_type: str,
    tokens: int, cost: Decimal,
    idempotency_key: Optional[str] = None,   # اختياري
    request_tokens: int = 0, completion_tokens: int = 0
) -> bool

# ai_agents/service.py:147-153
async def execute_agent_action(
    self, agent_id: int, action_type: str, payload: Dict[str, Any],
    executor_user_id: int, idempotency_key: str    # 🔴 إجباري، بلا default
) -> Dict[str, Any]
```

الفرق الجوهري بين الاتنين: `check_and_consume`'s `idempotency_key` **اختياري** (`= None`)، لكن `execute_agent_action`'s `idempotency_key` **إجباري بلا default** — هذا الفرق البنيوي هو سبب رئيسي لاختلاف نمط الأعطال بين #15 و#16.

---

## Backlog #12 — `can_access_service` — ✅ متطابق تمامًا (6/6 بسيط)

| الدومين | الاستدعاء الحالي | التصنيف |
|---|---|---|
| `service_marketplace` | `can_access_service(tenant_id, "service_marketplace")` | 🟢 بسيط |
| `social` | `can_access_service(tenant_id, feature)` | 🟢 بسيط |
| `tenders_auctions` | `can_access_service(tenant_id, feature)` | 🟢 بسيط |
| `tourism_sports` | `can_access_service(tenant_id, feature)` | 🟢 بسيط |
| `transport` | `can_access_service(tenant_id, feature)` | 🟢 بسيط |
| `zamakana` | `can_access_service(tenant_id, feature)` | 🟢 بسيط |

**كل الستة مواضع بنفس الشكل الحرفي تمامًا:** معاملين موضعيين (`tenant_id`, `feature`)، والتوقيع الحقيقي بياخد واحد بس. الإصلاح: حذف `tenant_id` من الاستدعاء — **صفر استثناء، صفر حالة مركّبة**. **هذا البند الوحيد من التلاتة اللي فعلاً جلسة ميكانيكية واحدة موحّدة تكفيه بالكامل.**

---

## Backlog #15 — `check_and_consume` — ❌ غير متطابق (6 بسيط / 7 مركّب من أصل 13)

| الدومين | الاستدعاء | `action_type` موجود؟ | التصنيف |
|---|---|---|---|
| `invitations` (سطر 381) | ✅ `action_type="CRM_CHAT"` | نعم | 🟢 بسيط |
| `insurance` (سطر 332) | ✅ `action_type="CLAIM_ANALYSIS"` | نعم | 🟢 بسيط |
| `service_marketplace` (سطر 159) | ✅ `action_type="SERVICE_PURCHASE"` | نعم | 🟢 بسيط |
| `manufacturing` (سطر 298) | ✅ `action_type="MANUFACTURING_ANALYSIS"` | نعم | 🟢 بسيط |
| `manufacturing` (سطر 660) | ✅ `action_type="MAINTENANCE_ANALYSIS"` | نعم | 🟢 بسيط |
| `logistics` (سطر 544) | ✅ `action_type="LOGISTICS_FORECAST"` | نعم | 🟢 بسيط |
| `zamakana` (سطر 495) | ❌ غير ممرَّر إطلاقًا | **لا** | 🔴 **مركّب** |
| `arbitration_syndicates` (سطر 93) | ❌ غير ممرَّر إطلاقًا | **لا** | 🔴 **مركّب** (موثَّق سابقًا كـ"باج مركّب") |
| `transport` (سطر 74) | ❌ غير ممرَّر إطلاقًا | **لا** | 🔴 **مركّب** (لم يُكتشَف سابقًا — اكتشاف جديد في هذا التدقيق) |
| `tourism_sports` (سطر 426) | ❌ غير ممرَّر إطلاقًا | **لا** | 🔴 **مركّب** (اكتشاف جديد) |
| `tenders_auctions` (سطر 202) | ❌ غير ممرَّر إطلاقًا | **لا** | 🔴 **مركّب** (اكتشاف جديد) |
| `social` (سطر 304) | ❌ غير ممرَّر إطلاقًا | **لا** | 🔴 **مركّب** (اكتشاف جديد) |
| `realestate` (سطر 74) | ❌ غير ممرَّر إطلاقًا | **لا** | 🔴 **مركّب** (اكتشاف جديد) |

**🔴 اكتشاف جديد مهم:** الجلسة السابقة كانت وثّقت `arbitration_syndicates` بس كـ"حالة مركّبة استثنائية" (بارزة، فريدة). **التدقيق الفعلي هنا يثبت إنها مش استثناء — 7 من أصل 13 موضع (54%) مركّبة بنفس الشكل بالضبط** (نفس الغياب الكامل لـ`action_type`). ده معناه لو جلسة مركزية اتعملت لـ#15 بافتراض "الإصلاح بسيط زي #12″، **7 مواضع من 13 كانت هتفضل تكراش بعد الإصلاح** (`TypeError: missing 1 required positional argument: 'action_type'`) رغم حذف `tenant_id` بنجاح.

**الإصلاح المركّب مش ميكانيكيًا بحتًا:** كل موضع مركّب يحتاج **قرار عمل** (إيه القيمة المناسبة لـ`action_type` في سياق هذا الدومين تحديدًا؟) — مقارنة بالمواضع البسيطة اللي بالفعل عندها قيمة جاهزة (`"CLAIM_ANALYSIS"`, `"MANUFACTURING_ANALYSIS"`, إلخ) محتاجة بس حذف سطر.

---

## Backlog #16 — `execute_agent_action` — ❌ غير متطابق (5 بسيط / 13 مركّب من أصل 18)

| الدومين | الموضع | `idempotency_key` ممرَّر؟ | التصنيف |
|---|---|---|---|
| `insurance` | سطر 340 | ✅ `cast(str, idempotency_key)` | 🟢 بسيط |
| `employment` | سطر 140 | ✅ `idempotency_key=idempotency_key` | 🟢 بسيط |
| `automation` | سطر 707 | ✅ `idempotency_key=idempotency_key` | 🟢 بسيط |
| `logistics` | سطر 556 | ✅ `idempotency_key or ""` | 🟢 بسيط |
| `arbitration_syndicates` | سطر 103 | ✅ `f"AI-{idempotency_key}" if ... else f"AI-{uuid4()...}"` | 🟢 بسيط (لكن منطق fallback مخصَّص) |
| `zamakana` | سطر 518 | ❌ غير ممرَّر | 🔴 **مركّب** |
| `invitations` | سطر 82 | ❌ غير ممرَّر | 🔴 **مركّب** |
| `invitations` | سطر 413 | ❌ غير ممرَّر | 🔴 **مركّب** |
| `insurance` | سطر 433 | ❌ غير ممرَّر | 🔴 **مركّب** |
| `transport` | سطر 165 | ❌ غير ممرَّر | 🔴 **مركّب** |
| `tourism_sports` | سطر 268 | ❌ غير ممرَّر | 🔴 **مركّب** |
| `tourism_sports` | سطر 408 | ❌ غير ممرَّر | 🔴 **مركّب** |
| `tenders_auctions` | سطر 210 | ❌ غير ممرَّر | 🔴 **مركّب** |
| `tenders_auctions` | سطر 329 | ❌ غير ممرَّر | 🔴 **مركّب** |
| `social` | سطر 314 | ❌ غير ممرَّر | 🔴 **مركّب** |
| `realestate` | سطر 230 | ❌ غير ممرَّر | 🔴 **مركّب** |
| `manufacturing` | سطر 309 | ❌ غير ممرَّر | 🔴 **مركّب** |
| `manufacturing` | سطر 671 | ❌ غير ممرَّر | 🔴 **مركّب** |

**72% من مواضع #16 مركّبة** (13 من 18) — نسبة أعلى حتى من #15. **لكن هنا فيه طبقة تعقيد إضافية**: الـ13 موضع المركّب مش كلهم بنفس درجة الصعوبة — لأن `idempotency_key` (بعكس `action_type`) أحيانًا موجودة أصلًا كباراميتر متاح في نفس الدالة (بس مش ممرَّرة للاستدعاء)، وأحيانًا **مش موجودة إطلاقًا** في الدالة المحيطة:

| الدومين:الدالة | `idempotency_key` متاحة كباراميتر؟ | نوع الإصلاح |
|---|---|---|
| `zamakana.generate_ai_analysis` | ✅ متاحة (`Optional[str] = None`) | تمرير مباشر + fallback لأنها Optional والتوقيع الحقيقي يطلب str إجباري |
| `tourism_sports.purchase_event_ticket` | ✅ متاحة | تمرير مباشر |
| `manufacturing.start_production` | ✅ متاحة | تمرير مباشر |
| `realestate.buy_fractional_ownership` | ✅ متاحة | تمرير مباشر |
| `tenders_auctions.evaluate_bid_technically` / `place_bid` | ✅ متاحة (الاتنين) | تمرير مباشر |
| `invitations._analyze_target_user(user_id, tenant_id)` | ❌ **غير متاحة إطلاقًا** — الدالة معندهاش باراميتر `idempotency_key` من الأساس | **يحتاج توليد قيمة جديدة من الصفر** (مش مجرد تمرير) |
| `social.get_match_suggestions(user_id, tenant_id, limit)` | ❌ **غير متاحة إطلاقًا** | **يحتاج توليد قيمة جديدة من الصفر** |
| `manufacturing.analyze_and_schedule_maintenance(user_id, tenant_id, production_line_id, sensor_data)` | ❌ **غير متاحة إطلاقًا** | **يحتاج توليد قيمة جديدة من الصفر** |
| `transport.create_route(tenant_id, data)` | ❌ **غير متاحة إطلاقًا** | **يحتاج توليد قيمة جديدة من الصفر** |

**يعني حتى داخل فئة "مركّب" الواحدة، فيه فئتان فرعيتان مختلفتان فعليًا في الصعوبة:**
- **(أ) مركّب-سهل:** القيمة متاحة أصلًا في الـscope، بس مش ممرَّرة — شبه ميكانيكي.
- **(ب) مركّب-صعب:** القيمة مش موجودة إطلاقًا في الدالة المحيطة — يحتاج قرار تصميمي (توليد `uuid`؟ تركيب string من معرّفات تانية زي اللي عمله `arbitration_syndicates`؟) لكل موضع على حدة.

---

## الخلاصة: تصحيح رسمي لملف `constructor-mismatch-backlog-classification.md`

| البند | الادعاء السابق | الواقع المؤكَّد بالقراءة المباشرة | القرار الصحيح |
|---|---|---|---|
| #12 | "نفس السبب الجذري، نفس الإصلاح" | ✅ **صحيح فعلاً** — 6/6 بسيط، 100% متطابق | جلسة ميكانيكية واحدة، **بدون تعديل** |
| #15 | "نفس السبب الجذري، نفس الإصلاح لكل الدومينات" | ❌ **غير دقيق** — 7/13 (54%) مركّبة، تحتاج قرار عمل لكل واحدة | **يُقسَّم لجلستين فرعيتين**: (1) الـ6 البسيطة كإصلاح ميكانيكي سريع، (2) الـ7 المركّبة كمراجعة فردية لكل دومين (تحديد `action_type` المناسب) |
| #16 | "نفس السبب الجذري، نفس الإصلاح" | ❌ **غير دقيق أكتر من #15** — 13/18 (72%) مركّبة، ومنها 4 (على الأقل) تحتاج توليد قيمة من الصفر مش مجرد تمرير | **يُقسَّم لثلاث مجموعات فرعية**: (1) 5 بسيطة، (2) مركّب-سهل (القيمة متاحة، تمرير بس)، (3) مركّب-صعب (توليد قيمة جديدة — قرار تصميمي لكل دومين) |

**الإجابة المباشرة على السؤال:** **لأ، مش متطابق تقنيًا** — التجميع المقترَح سابقًا لـ#12+#15+#16 في "جلسة مركزية واحدة" **كان بيفترض تجانس غير موجود فعليًا**. تجميعهم في جلسة واحدة حرفيًا **هيزوّد التعقيد فعلاً بدل ما يقلله**، لأن:
1. حجم العمل مختلف جوهريًا (#12: حذف سطر واحد × 6 مواضع. #15/#16: حذف + قرارات عمل متفرقة × عدد أكبر من المواضع).
2. مخاطر الإصلاح مختلفة — الإصلاح الميكانيكي (#12) صفر مخاطرة منطقية، لكن اختيار قيمة `action_type`/توليد `idempotency_key` (#15/#16 المركّبة) قرار له أثر على منطق العمل (مثلاً: `idempotency_key` مولَّدة بشكل خاطئ ممكن تكسر ضمان عدم التكرار الفعلي للعملية).
3. مراجعة/اختبار كل نوع مختلف — التغيير الميكانيكي يتحقق بـ`git diff` + `py_compile` بس، لكن التغيير المركّب يحتاج تحقق حي فعلي لكل دومين على حدة (زي ما حصل فعلاً مع `arbitration_syndicates`).

### التوصية النهائية

- **#12:** جلسة واحدة قصيرة، ميكانيكية بالكامل — 6 مواضع، صفر قرارات.
- **#15 و#16 مع بعض:** **مش جلسة واحدة، لكن 3 جلسات فرعية متتالية** (يمكن في نفس اليوم، لكن بفواصل واضحة):
  1. **جلسة أ (ميكانيكية):** كل المواضع البسيطة في #15 و#16 مجتمعة (6+5 = 11 موضع) — حذف `tenant_id` بس، صفر قرارات.
  2. **جلسة ب (مركّب-سهل):** المواضع اللي محتاجة إضافة `action_type`/`idempotency_key` من قيمة **موجودة أصلًا في نفس الدالة** — تحتاج مراجعة فردية خفيفة لكل موضع (تأكيد القيمة المناسبة) لكن بدون تصميم جديد.
  3. **جلسة ج (مركّب-صعب):** المواضع اللي محتاجة توليد قيمة `idempotency_key` من الصفر (`invitations._analyze_target_user`, `social.get_match_suggestions`, `manufacturing.analyze_and_schedule_maintenance`, `transport.create_route`) + تحديد `action_type` مناسب لكل دومين في #15 المركّبة — هذه الجلسة الوحيدة اللي فعلاً تحتاج وقت/تفكير مخصَّص لكل دومين، ويُفضَّل معاملتها كأولوية أقل من (أ) و(ب).
