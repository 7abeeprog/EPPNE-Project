# ملخص Backlog نهائي (18 بند) — تصنيف: جلسة تصحيح مركزي واحدة مقابل إصلاح محلي لدومين واحد

**تاريخ الإعداد:** 2026-08-17
**الغرض:** الإجابة على السؤال "اجمع ملخص Backlog نهائي (الآن 18 بند) مع تمييز واضح: أيها يحتاج جلسة تصحيح مركزي واحدة (زي #14 audit_log، #11 InvoicingService) مقابل أيها محلي لدومين واحد".
**المصدر:** قائمة الـBacklog الكاملة (البند 1 إلى 18) في `PROGRESS_LOG.md`، قسم "📋 قائمة انتظار الجلسات المستقبلية"، مع التحديثات اللاحقة المُلحَقة بكل بند. لم يُضَف أي بند جديد هنا — هذا الملف تصنيف/تجميع فقط، صفر تعديل على المحتوى الأصلي.

---

## معيار التصنيف

| الفئة | التعريف | الإصلاح المطلوب |
|---|---|---|
| 🔴 **مركزي** | سبب جذري واحد (توقيع method تغيَّر، أو method/عمود مفقود في كلاس/موديل مشترك) يتكرر بنفس الشكل عبر **عدة دومينات مستقلة** (call sites مختلفة، كود مختلف حرفيًا لكل واحد) | إصلاح **الكلاس/الدالة المشتركة نفسها مرة واحدة** (أو `grep` شامل + تعديل كل الاستدعاءات دفعة واحدة في جلسة واحدة) — إصلاح دومين بمفرده لا يحل المشكلة في الباقي |
| 🟢 **محلي** | خطأ مرتبط بدومين واحد تحديدًا (منطق عمل خاص، أو موديل/schema خاص بدومين واحد) — لا يوجد دليل أو احتمال منطقي لتكراره في دومين تاني بنفس الشكل | إصلاح الموضع/الدومين المحدَّد فقط، لا يستدعي جلسة عابرة للدومينات |
| 🟡 **غير مؤكَّد الانتشار** | ثبت حدوثه في موضع واحد على الأقل، لكن لم يُنفَّذ `grep`/جرد شامل لتأكيد أو نفي وجوده في دومينات أخرى | يحتاج **جلسة جرد قصيرة أولاً** (`grep` بس، بدون إصلاح) لتحديد الفئة الحقيقية قبل تقرير حجم الجلسة اللازمة |
| ⚪ **ليس فئة باج** | بند إداري/قرار منتجي أو استكمال جلسة قائمة، مش نمط باج جديد قابل للتصنيف مركزي/محلي | يُدار حسب طبيعته الخاصة، غير مشمول بمعيار مركزي/محلي |

---

## الجدول الموحّد

| # | الاسم | الفئة | عدد الدومينات المؤكَّدة | ملاحظة الأولوية |
|---|---|---|---|---|
| 1 | `user-repository-get-by-id-audit` | 🔴 مركزي | 13 دومين (15 موضع) | فشل مالي/عمولة **صامت** مؤكَّد حيًا (`service_marketplace`) — أولوية مرتفعة. **✅ مُصلَح ومُغلَق رسميًا [2026-08-19]** — `tenant_id` أُضيف كمعامل ثانٍ في كل الـ15 موضع. دليل حي مقتبس من `.claude/reports/user-repository-get-by-id-audit-session-log.md` §16: *"`tests/test_user_repository_get_by_id_audit.py` — 15 اختبارًا ... 15 passed × تشغيلتين متتاليتين، صفر تذبذب، صفر بيانات throwaway متبقية"* — تحقق حي بمعاملين (صحيح/خاطئ) لكل موضع يثبت عزل tenant فعلي، مش بس اختفاء الخطأ. |
| 2 | `duplicate-kwarg-audit` | 🔴 مركزي | 4+ دومينات | مؤكَّد سابقًا في جلسة `simpletenant-fix`. **✅ تحديث [2026-08-26]: موضعان امتداد لنفس البند (`commerce.create_payment_request`, `sovereign_entities.create_entity`) اتصلحوا وتحقَّقوا حيًا في جلسة `constructor-mismatch-backlog-cleanup` (commit `55032eb`, 2026-08-25).** دليل حي مقتبس حرفيًا من `.claude/reports/constructor-mismatch-backlog-cleanup-session-log.md`: `commerce` — *"أُنشئ order throwaway (id=18، `PENDING_PAYMENT`)، `create_payment_request` نجح (`id=3, tenant_id=1`)، اتنضف (`payment_requests` + `orders`)."*؛ `sovereign_entities` — *"إنشاء entity throwaway حقيقي (`id=21`, `entity_type=ENTERPRISE`) نجح، اتنضف (`entity_pages` + `sovereign_entities_v2`)."* **النطاق الأوسع للبند (الـ4+ دومينات الأصلية) غير مُتحقَّق هنا — هذا إغلاق للموضعين الممتدين فقط.** |
| 3 | Phase 16 الأصلي (استكمال) | ⚪ ليس فئة باج | — | استكمال جلسة قائمة، مش نمط باج جديد |
| 4 | `silent-write-regression` (المتبقي) | ⚪ ليس فئة باج (متبقي من جلسة مركزية سابقة) | 1 دومين (`saas`، موضعان) | إغلاق أخير لجلسة `silent-write-regression-session-log.md` القائمة أصلًا |
| 5 | `sovereign_entities` (4 endpoints بلا مصادقة) | 🟢 محلي | 1 دومين | قرار منتجي معلَّق، مش باج تقني عابر للدومينات. **✅ القرار اتحسم ومُصلَح [2026-08-24، جلسة `sovereign-entities-idor-fix`]** — الأربعة صاروا محميين بـ`get_current_active_user` إجباري. دليل حي مقتبس من `.claude/reports/sovereign-entities-idor-fix-session-log.md` §9.3: *"طلب مجهول تمامًا (بلا أي `Authorization` header) على الأربعة كلهم: `GET /api/sovereign-entities/ → 401`, `GET /api/sovereign-entities/2 → 401`, `GET /api/sovereign-entities/templates → 401`, `GET /api/sovereign-entities/components → 401`"* (بعد ما كانت `500` بلا أي حاجز مصادقة). |
| 6 | `commerce.visa_webhook` | 🟢 محلي | 1 دومين | مراجعة أمنية مخصَّصة لبوابة دفع واحدة. **✅ مُصلَح [2026-08-25، commit `b55b57c`]** — اشتقاق `tenant_id` من الطلب نفسه بدل هيدر `X-Tenant-ID` غير موثوق + `get_current_superuser` كحارس مؤقت. دليل حي مقتبس من `.claude/reports/commerce-visa-webhook-fix-session-log.md` §9: *"بلا أي توكن (نفس هجوم الفجوة الأصلية) → `401 "Not authenticated"` — الباب المفتوح بالكامل مُغلَق"*، و*"سوبريوزر حقيقي، `gateway_reference` مطابق، `status="SUCCESS"` → تحقق DB مستقل: `orders.status → PAID`، `payment_requests.status → PAID`"*. |
| 7 | `redis-client-wrapper-missing-methods` | 🔴 مركزي | 2 مؤكَّدة (`ai_agents`, `projects`) + احتمال انتشار غير مفحوص | `RedisClientWrapper` كلاس مشترك — نفس السبب الجذري (subset ناقص من مكتبة `redis`). **✅ مُصلَح ومُغلَق رسميًا [2026-08-19]** — 6 methods أُضيفت (`hincrbyfloat`, `hgetall`, `lpush`, `ltrim`, `setnx`, `pubsub`). دليل حي مقتبس من `.claude/reports/redis-client-wrapper-missing-methods-session-log.md`: *"hincrbyfloat: PASS ... independent_hget: '3.75' ← HGET مستقل عبر العميل الخام، يطابق التراكم"*، *"lpush: PASS ... independent_lrange: ['v3','v2','v1'] ← LRANGE مستقل، ترتيب صحيح"* — كل method اتفحصت بأداة تحقق مستقلة عن أداة النداء نفسها. |
| 8 | `user-repository-get-user-audit` | 🔴 مركزي | 5 دومينات (6 مواضع) | `UserRepository.get_user()` غير موجودة أصلًا — نفس الاستدعاء الخاطئ متكرر حرفيًا. **✅ مُصلَح ومُغلَق رسميًا [2026-08-19]** — 5 مواضع فعليًا (8 نقاط استدعاء حية)، زائد method جديدة `get_tenant_id_by_user_id`. دليل حي مقتبس من `.claude/reports/user-repository-get-user-audit-session-log.md`: *"12/12 تحقق نجح، صفر فشل"* — يشمل `digital_twin._get_user(WRONG tenant -> None)`، `employment._get_user_email(correct tenant)`، و`_register_affiliate_commission` لكلا الدومينين بلا استثناء. |
| 9 | `saas-control-service-missing-methods` | 🔴 مركزي | 2+ مؤكَّدة (`digital_twin`, `employment`) | 🔴🔴 **محظور الإصلاح قبل مراجعة تقرير `invitations` الحرج** — راجع تحذير مخصَّص أسفل. **✅ القيد اتشال ومُصلَح رسميًا [2026-08-18، بعد #11]** — `get_active_subscription(tenant_id)` أُضيفت على `SaaSControlService`. دليل حي مقتبس من `.claude/reports/saas-control-service-fix-session-log.md` §13.3/14.1: *"الرقم الحاسم: 4 من 4 دوال أثبتت حيًا إن `_check_saas_limits` ... بقت شغالة فعليًا بلا استثناء — 100%"*، و*"`realestate.rent_unit` ... تحقق حي كامل، نجاح تام"*. النطاق الفعلي المُصلَح: 8 دومينات (`realestate`, `insurance`, `digital_twin`, `employment`, `arbitration_syndicates`, `invitations`, `manufacturing`, `logistics`). |
| 10 | `affiliate-service-missing-methods` | 🔴 مركزي | 6+ دومينات (كل دومين بيستخدم `_register_affiliate_commission`) | إصلاح الكلاس مرة واحدة يحل كل الدومينات دفعة واحدة |
| 11 | `realestate-invoicing-savepoint-conflict` (+ امتداد identity) | 🔴 مركزي | 4+ دومينات (`realestate`, `arbitration_syndicates`, `insurance`, `invitations`) | 🔴🔴 **أعلى أولوية في كل الـBacklog** — جلسة `invitations-user-registration-savepoint-leak` مقررة، تُنفَّذ التالية مباشرة. **✅ مُصلَح ومُغلَق رسميًا [2026-08-18]، بشقيه:**<br>**#11a** (`invitations.accept_invitation` — يوزر بلا محفظة) — دليل حي مقتبس من `.claude/reports/invitations-savepoint-leak-session-log.md` §7-8: *"السيناريو النظيف: يوزر واحد **بمحفظة كاملة**، دعوة **ACCEPTED**، lead **CONVERTED** — بدل الكراش الأصلي عند `db.refresh()`."* و*"المحاولة الثانية (retry) نجحت فعليًا وأكملت القبول ... باستخدام نفس اليوزر بالظبط (`id=72` مش يوزر جديد) — صفر ازدواج، صفر يوزر يتيم متروك."*<br>**#11b** (`invoicing.create_invoice` جوه `begin_nested()`، 4 دومينات) — دليل حي مقتبس من `.claude/reports/realestate-insurance-savepoint-fix-session-log.md` §15.2/15.4: *"`realestate.rent_unit` ... تحقق حي كامل ونظيف 100%"* (نظيف + فشل مُفتعَل، تشغيلتان)، و*"الجزء اللي بيخص #11b تحديدًا (التزام `commit()` + سلوك `create_invoice()` الصحيح) مؤكَّد بالكامل بـSELECT مستقل، تمامًا زي `rent_unit`"* لـ`insurance.subscribe`. **تحفظ صريح موثَّق بالمصدر: تحقق حي كامل لدالتين فقط من الأربعة (`rent_unit`, `subscribe`) + `purchase_event_ticket`/`zamakana.pledge_time` (راجع PROGRESS_LOG.md #11b) — `buy_fractional_ownership`/`review_claim` مراجعة ديف/كود فقط، محجوبتان ببج `tx_hash`/`Transaction` منفصل (#37 أعلى).** |
| 12 | `saas-control-service-wrong-arity-call` | 🔴 مركزي | 6+ دومينات (`service_marketplace`, `social`, `tenders_auctions`, `tourism_sports`, `transport`, `zamakana`) | نفس التوقيع الجديد لـ`can_access_service`، نفس الإصلاح يطبَّق حرفيًا في كل دومين. **✅ مُصلَح [2026-08-26، commit `55032eb`، جلسة `constructor-mismatch-backlog-cleanup`]** — حذف `tenant_id=` الزايدة من الخمس دومينات (`social` كانت سليمة أصلًا). دليل حي مقتبس من `.claude/reports/constructor-mismatch-backlog-cleanup-session-log.md`: *"نداء مباشر على الخمسة عبر `SaaSControlService(db, 1)` — صفر `TypeError`، كلهم رجعوا `bool` (معظمهم `False` لعدم وجود اشتراك فعّال، وهو سلوك صحيح متوقَّع مش باج)."* |
| 13 | `invoicing-create-invoice-wrong-kwarg` (الجزء الخاص باسم الـkwarg نفسه) | 🟡 غير مؤكَّد الانتشار | 1 مؤكَّد (`service_marketplace`) | يحتاج `grep` شامل لباقي الـ12 دومين المستخدمة لـ`InvoicingService` — **امتداده الداخلي (كراش `audit_log` جوه `create_invoice`) مغطّى بالفعل تحت #14 كمركزي 100%** |
| 14 | `audit-log-wrong-kwargs` | 🔴 مركزي (الأوسع انتشارًا في كل الـBacklog) | عشرات المواضع عبر كل دومين تقريبًا + **داخل `InvoicingService.create_invoice()` نفسها** | المثال المرجعي اللي طلبته — إصلاح دالة `audit_log()` الواحدة (أو تحديث كل الاستدعاءات دفعة واحدة) يحل كل الدومينات فورًا. **✅ مُصلَح ومُغلَق رسميًا [2026-08-18]** — توسيع توقيع `audit_log()` ليقبل `tenant_id`/`resource_id` اختياريين، صفر migration. الرقم المصحَّح: 95 موضع فعليًا عبر 18 ملف (مش ~112/22). دليل حي مقتبس من `.claude/reports/audit-log-fix-session-log.md` §10 (جدول التحقق، عيّنة 4 دومينات): *"`communications/router.py` (`MAIL_MOVE_TO_TRASH`) ... `audit_log()` نجحت؟ ✅ ... SELECT مستقل يثبت الحفظ: ✅ `(1, 'TRASH')` من `mailbox_items` — الـfolder اتغيّر فعليًا"* — يثبت اختفاء الـ`500`/rollback الصامت، مش بس صفر استثناء. |
| 15 | `ai-governance-check-and-consume-wrong-kwarg` | 🔴 مركزي | 8+ دومينات | بعضها محمي (`insurance`, `tenders_auctions`, `transport`)، بعضها **غير محمي** (`arbitration_syndicates`, `manufacturing`, `logistics`, `social`, `zamakana`) — الإصلاح المركزي واحد لكل الحالتين. **✅ الجزء الأساسي مُصلَح ومُغلَق رسميًا [2026-08-18]** — 13/13 موضع: `tenant_id=` الزائدة أُزيلت، 7/13 كان عندهم `action_type` الإجباري ناقص كمان (أُضيف). دليل حي مقتبس من `.claude/reports/ai-governance-agents-fix-session-log.md` §17-18: *"تحقق حي كامل ... أثبت: (أ) الاستدعاءات الحقيقية تعمل بدون استثناء، (ب) فحص الحصة يحسب الاستهلاك صح، (ج) القيم الجديدة لـ`action_type` تُسجَّل بدقة، (د) الرفض الفعلي عند تجاوز الحصة يعمل"*. **تحفظ:** بند فرعي منفصل `ai-governance-usage-log-idempotency-wrong-arity` (اكتُشف 2026-08-19، مؤكَّد لسه موجود 2026-08-25) **لسه مفتوح** — استدعاء داخلي مختلف تمامًا جوه نفس الدالة، خارج نطاق هذا الإصلاح. |
| 16 | `ai-agents-execute-agent-action-wrong-kwarg` | 🔴 مركزي | 9+ دومينات | أغلبها محمي بـ`try/except` (فشل هادئ) — نفس السبب الجذري، نفس الإصلاح لكل الدومينات |
| 17 | `arbitration-case-model-idempotency-key-mismatch` | 🟢 محلي | 1 دومين (`arbitration_syndicates`) | مشكلة schema في موديل `ArbitrationCase` تحديدًا — لا سبب جذري مشترك مع أي دومين تاني. **✅ مُصلَح [2026-08-26، commit `55032eb`، migration `037_add_idempotency_key_to_arbitration_cases`]** — عمود `idempotency_key` أُضيف. دليل حي مقتبس من `.claude/reports/constructor-mismatch-backlog-cleanup-session-log.md`: *"`ArbitrationSyndicatesRepository.create_case(..., idempotency_key="VERIFY_TEST_...")` نجح (`id=1`)، اتنضف."* |
| 18 | `finance-service-create-invoice-does-not-exist` | 🟢 محلي (مبدئيًا) | 1 دومين (`transport`، موضعان) | خطأ نسخ/استدعاء محلي (استدعاء method مش موجودة على `FinanceService`) — لم يُفحَص شموليًا لكن لا يوجد سبب جذري "توقيع تغيَّر" يبرر التكرار. **🟡 الكود مُصلَح [2026-08-26، commit `55032eb`] لكن بلا تحقق حي كامل — تحفظ صريح موثَّق في المصدر نفسه.** `transport` رُبِط بـ`InvoicingService` (نفس نمط باقي الدومينات، بعد `commit()` الخارجي). دليل مقتبس من `.claude/reports/constructor-mismatch-backlog-cleanup-session-log.md`: *"جداول دومين `transport` بالكامل ... غير موجودة إطلاقًا في قاعدة البيانات الحالية ... استحال بناء سيناريو حجز/توصيل حقيقي كامل للتحقق الحي التقليدي. التحقق البديل المُنفَّذ: استيراد الملف نجح بلا أخطاء ... فحص السورس المصدري ... أكَّد: صفر أثر لـ`finance.create_invoice` (الباج القديم)، ووجود `invoicing.create_invoice` في الموضعين."* — **هذا البند يستاهل تحقق حي كامل لاحقًا لو/لما جداول `transport` اتعملها migration**، بعكس باقي بنود هذه المجموعة اللي اتحقَّقت حيًا بالكامل. |
| 19 | `projects-schema-model-field-mismatch` [أُضيف 2026-08-24، بعد تجميد هذا الملف] | 🟢 محلي | 1 دومين (`projects`، 3 موديلات: `Project`, `Contribution`, `ProjectUpdate`) | فئة فرعية جديدة من `constructor-mismatch` — عدم تطابق **schema↔model** (حقول Pydantic بلا أعمدة SQLAlchemy مقابلة)، مش duplicate/unexpected-kwarg زي #2/#12/#15/#16. يمنع `create_project` بالكامل، و5 من 6 أنواع `add_contribution`، و`add_project_update` بالكامل. مُكتشَف أثناء التحقق الحي لجلسة `projects-idor-fix` — راجع `PROGRESS_LOG.md` (جدول الـBacklog النشط) و`.claude/reports/projects-idor-fix-session-log.md` §4.1. **✅ مُصلَح [2026-08-26، commit `55032eb`، migration `039_expand_projects_contributions_updates_schema`]** — الموديلات الثلاثة اتوسَّعت (28 عمود جديد) لمطابقة الـschema الموجود. دليل حي مقتبس من `.claude/reports/constructor-mismatch-backlog-cleanup-session-log.md`: *"`add_contribution` — **كل الـ6 أنواع** ... رجعوا نتيجة صحيحة (`equivalent_value_mrusdt` محسوبة لكل نوع)."* و*"`create_project` بحقول موسَّعة ... نجح (`id=6`)"* و*"`add_project_update` بـ`media_urls=[...]` — نجح."* |
| 20 | `academy-create-bootcamp-instructor-id-mismatch` [أُضيف 2026-08-24، بعد تجميد هذا الملف] | 🟢 محلي | 1 دومين (`academy`، موديل `Bootcamp`) | نفس فئة #19 (schema↔model) — `AcademyRepository.create_bootcamp()` بتمرر `instructor_id` لـ`Bootcamp(**kwargs)`، لكن جدول `academy_bootcamps` الفعلي **بلا عمود `instructor_id` إطلاقًا** (تأكيد `\d academy_bootcamps` حي). `TypeError` فوري، `POST /academy/bootcamps` معطوبة بالكامل لأي طلب. مُكتشَف أثناء التحقق الحي لجلسة `academy-idor-fix` — راجع `.claude/reports/academy-idor-fix-session-log.md` §1/§3.4. **✅ مُصلَح [2026-08-26، commit `55032eb`، migration `038_add_instructor_id_to_academy_bootcamps`]** — عمود `instructor_id` أُضيف. دليل حي مقتبس من `.claude/reports/constructor-mismatch-backlog-cleanup-session-log.md`: *"`AcademyService(db, 1).create_bootcamp(data, instructor_id=2)` نجح (`id=3, instructor_id=2`)، اتنضف."* |
| 21 | `invoicing-invoice-response-metadata-collision` [أُضيف 2026-08-25، بعد تجميد هذا الملف] | 🟢 محلي | 1 دومين (`invoicing`، `InvoiceResponse`) | فئة مختلفة عن #19/#20 (schema↔model) — `InvoiceResponse.metadata` (حقل Pydantic) يصطدم بخاصية `metadata` المحجوزة في SQLAlchemy `Base` بدل عمود `invoice_metadata` الفعلي، فيفشل `model_validate(from_attributes=True)` بـ`400` مضلِّل **لكل استدعاء `POST /invoicing/invoices` بلا استثناء** — رغم أن الكتابة في DB تنجح فعليًا قبل هذا الخطأ (نفس نمط "رد مضلِّل مع نجاح كتابة حقيقية" الموثَّق سابقًا في `commerce.handle_visa_webhook`/`ai_governance.check-and-consume`). مُكتشَف أثناء التحقق الحي لجلسة `batch1-audit-security-employment-invitations-communications-invoicing` — راجع `.claude/reports/batch1-audit-security-employment-invitations-communications-invoicing.md` §4-أ |
| 22 | `invitations-leads-route-ordering` [أُضيف 2026-08-25، بعد تجميد هذا الملف] | 🟢 محلي | 1 دومين (`invitations`، `router.py`) | فئة مختلفة تمامًا (باج ترتيب تسجيل routes، مش constructor/schema) — نفس نمط `sovereign_entities.list_templates/list_components` الموثَّق سابقًا: `GET /{invitation_id}` مُسجَّلة قبل `GET /leads` (وعلى الأرجح `/campaigns`/`/tickets`) في نفس الملف، فأي طلب لـ`/invitations/leads` يقع تحت نمط `/{invitation_id}` (int) أولًا ويُرفض بـ`422` قبل ما يوصل للهاندلر الحقيقي. **لا يلغي خطورة IDOR الأصلية** — القراءة/الكتابة المباشرة بالـID (`GET/PUT /leads/{id}`) شغالة بمعزل عن هذا الباج. مُكتشَف أثناء التحقق الحي لنفس الجلسة أعلاه — راجع نفس التقرير §4.2. **✅ مُصلَح [2026-08-29] — النطاق طلع أوسع من الموصوف:** مش `/leads` بس — `/stats`، `/campaigns`، `/tickets` كانوا محجوبين بنفس الآلية بالظبط (كل static-prefix route كان مُسجَّل بعد `/{invitation_id}`). الإصلاح: إعادة ترتيب كل الـrouter (نقل الأقسام 1-5 الثابتة قبل قسم `/{invitation_id}` الديناميكي). **تحقق حي مباشر عبر Starlette route matching الحقيقي** (`fastapi_app.routes`، مش تخمين): *"GET /api/invitations/leads -> matched route path='/api/invitations/leads', handler='list_leads'"* (بدل `handler='get_invitation'` قبل الإصلاح) — نفس التأكيد لـ`/stats`، `/campaigns`، `/tickets`، مع تأكيد إضافي إن `GET /api/invitations/999` لسه بيتوجّه صح لـ`get_invitation` (المسار الشرعي بلا كسر). |
| 23 | `bleach-clean-none-crash` [أُضيف 2026-08-25، بعد تجميد هذا الملف] | 🔴 **مركزي — بند منتشر مستقل، مفصول عمدًا عن #24-#26 (المحلية لـ`invitations`)** | **13 دومين، 52 موضع مؤكَّد عبر `grep`** | نمط عابر لـ13 دومين، **معاملته زي `duplicate-kwarg-audit` (#2)** — راجع القسم المخصَّص "🔴 بند منتشر مستقل — #23" مباشرة بعد هذا الجدول، فيه قائمة الـ13 دومين كاملة بعدد المواضع لكل واحد. صفر إصلاح الآن، توثيق موسَّع فقط |
| 24 | `invitations-create-invitation-fully-broken` [أُضيف 2026-08-25، بعد تجميد هذا الملف] | 🟢 محلي (لكن **مُركَّب من عطلين متتاليين**، أحدهما جزء من #23) | 1 دومين (`invitations`، `service.py`) | 🔴🔴 **`POST /invitations/` (إنشاء دعوة) معطوبة بالكامل حاليًا لأي طلب فعليًا ناجح — مش حافة نادرة، دي الوظيفة الأساسية للدومين اللي كله سُمي على اسمها.** عطلان متتاليان: **(أ)** `service.py:170-172` (`sanitized_title`/`sanitized_message`/`sanitized_identifier` عبر `bleach.clean(data.get(field,""),...)` بلا حارس) — نفس نمط #23، أول عائق يواجه أي طلب فيه `title`/`custom_message`/`target_entity_identifier` بقيمة `None` صراحة (الحالة الافتراضية لأي عميل ما حددش القيم دي). **حتى لو اتصلح #23**، عطل ثانٍ منفصل تمامًا يمنع نفس الـendpoint: **(ب)** `service.py:113` (`_assign_ai_agent`) — `agents[0]` على كائن `PaginatedResponse[AIAgentResponse]` (غير قابل للفهرسة بـ`[0]`) بدل قائمة خام، `TypeError: 'PaginatedResponse[AIAgentResponse]' object is not subscriptable`. **الإصلاح الصحيح لازم الاثنين معًا** — إصلاح (أ) وحده يوصّل الطلب لعطل (ب) فورًا، الـendpoint لسه معطوب. مؤكَّد حيًا بالتنفيذ الفعلي (تسلسل الأعطال ظهر بالترتيب ده بالضبط، مرتين، أثناء محاولة اختبار المسار الشرعي في جلسة `batch1-audit-security-...`) — راجع نفس التقرير §10.3 |
| 25 | `invitations-legacy-invitation-rows-response-validation-error` [أُضيف 2026-08-25، بعد تجميد هذا الملف] | 🟢 محلي — **✅ مُصلَح [2026-08-26]** | 1 دومين (`invitations`، جدول `sovereign_invitations_v2`) | صفوف throwaway قديمة جدًا من جلسات سابقة (`P-CTOR-INV-*`, `P-SAVEPOINT-FIX-*`، الصفوف `id=1,2,3`) اتكتبت قبل ما تتضاف أعمدة `discount_percentage`/`gift_coins_amount`/`gift_currency`/`click_count` كإلزامية (non-null) في الـschema الحالي — قيمتها `NULL` فعليًا في DB، فـ`GET /invitations/{id}` عليها يفشل بـ`ResponseValidationError` (4 أخطاء تحقق). **امتداد مؤكَّد [2026-08-26]:** نفس الباج بالضبط يظهر كمان على `GET /invitations/` (القائمة الكاملة، 6 أخطاء عبر عدة صفوف) — راجع `.claude/reports/frontend-url-prefix-comprehensive-audit-session-log.md` §12 صف 7. **✅ الإصلاح المُطبَّق [2026-08-26] (اختيار الـschema، مش migration/حذف):** `discount_percentage`/`gift_coins_amount` (`InvitationCreate`، موروثة في `InvitationResponse`) بقى عندهم `field_validator(mode="before")` يحوّل `None` → `Decimal("0.0")`؛ `click_count` (`InvitationResponse`) نفس الفكرة → `0`؛ `gift_currency` بقى `Optional[str]` (يسمح بـ`None` كما هو، بدل فرض قيمة وهمية). **تحقق حي بعد الإصلاح:** `GET /invitations/` → `200` (بدل `500`)، والصف المُشكِل نفسه (`id=1`, `P-CTOR-INV-ACCEPT-TEST`) رجع بـ`discount_percentage:"0.0"`, `gift_coins_amount:"0.0"`, `gift_currency:null`, `click_count:0`. راجع نفس التقرير §18 بند 6، §20 |
| 26 | `invitations-update-not-found-500` [أُضيف 2026-08-25، بعد تجميد هذا الملف] | 🟢 محلي (لم يُفحَص انتشاره خارج `invitations`) | 1 دومين مؤكَّد (`invitations`: `update_lead`, `update_ticket`) | تحديث مورد غير موجود (سواء لأنه ينتمي لتينانت تاني بعد إصلاح IDOR، أو لأنه غير موجود إطلاقًا حتى ضمن تينانت المستخدم — مؤكَّد بتجربة معزولة `PUT /leads/9999`) بيرجّع `500 Internal Server Error` بدل `404` نظيف. **آمن تمامًا من ناحية الأمان — صفر كتابة فعلية تحصل** (مؤكَّد بـ`SELECT` مستقل)، بس رمز الخطأ غلط ويكشف تفاصيل تنفيذ داخلية بدل رد نظيف. لم يُفحَص هل نفس النمط موجود في `update_invitation`/`update_campaign` (الأخيرة رجّعت `403` نظيف مش `500` في نفس الجلسة — يحتاج توضيح ليه الفرق) أو دومينات تانية. مؤكَّد حيًا (جلسة `batch1-audit-security-...` §10.2/10.3) |
| 27 | `arbitration-syndicates-repository-missing-methods` [أُضيف 2026-08-25، بعد تجميد هذا الملف] | 🟢 محلي — **✅ مُصلَح بالكامل [2026-08-29]** (6/6 دوال، راجع تفصيل تحت) | 1 دومين (`arbitration_syndicates`) | `service.py` كان بيستدعي 5 دوال `repository.py` غير موجودة إطلاقًا (`get_cases_by_claimant`, `get_election_votes`, `get_syndicate_memberships`, `list_syndicate_elections`, `list_user_licenses`) + دالتان معرَّفتان بتوقيع أضيق مما يُستدعى بهما (`list_user_cases(user_id)` مقابل استدعاء بوسيطين، `list_candidates(election_id)` مقابل استدعاء بوسيطين) — كان بيعطّل 4 endpoints بالكامل (`GET /cases/me`, `GET /syndicates/{id}/elections`, `GET /elections/{id}/candidates` بـ`TypeError`/`AttributeError`/500 مؤكَّد حيًا، `GET /licenses/me` بيرجّع `[]` دايمًا بصمت لأن `hasattr` بترجع `False`). محصور بموديل/repository دومين واحد — صفر سبب جذري مشترك مع أي دومين تاني. **✅ [2026-08-26] `list_user_cases` مُصلَحة** (اكتُشفت أثناء إصلاح باج آخر غير متعلق — كانت **بلا أي فلترة تينانت إطلاقًا**، ثغرة IDOR حقيقية سُدَّت في نفس الوقت. راجع `.claude/reports/critical-finding-xtenant-systemic.md` صف #6 و`.claude/reports/frontend-url-prefix-comprehensive-audit-session-log.md` §18 بند 2، §20). **✅ [2026-08-29] الباقي (`list_user_licenses`, `list_candidates`, `get_election_votes`, `get_syndicate_memberships`, `list_syndicate_elections` [دالة سادسة أُضيفت للنطاق أثناء التحقيق — كانت السبب الفعلي لعطل `GET /syndicates/{id}/elections`], + حذف الفرع الميت `get_cases_by_claimant`) — كل واحدة فُلترت بـ`tenant_id` فعليًا من أول تنفيذها ومؤكَّدة حيًا بسيناريو هجوم/شرعي/decoy متقاطع التينانت لكل دالة. صفر IDOR اتلقى أثناء هذه الجلسة. راجع `.claude/reports/arbitration-syndicates-repository-27-session-log.md`.** |
| 28 | `saas-service-catalog-missing-entries` [أُضيف 2026-08-25، بعد تجميد هذا الملف؛ **مُوسَّع 2026-08-26 بإضافة `zamakana`، راجع `batch5-audit-security-...`§7**] | 🟢 محلي (بيانات إعداد، مش كود) | **3 دومينات** (`service_marketplace`, `tenders_auctions`, `zamakana`) | `saas_service_catalog` **بلا أي صف** لـ`tenders`/`auctions`/`service_marketplace`/`zamakana` (أو مرادفاتها) — `SaaSControlService.can_access_service(code)` بترجع `False` دايمًا لأي تينانت مهما كانت خطته، لأن `get_service_by_code` نفسها بترجع `None`. يمنع تفعيل الدومينات الثلاثة بالكامل حاليًا (`POST /purchase`, `POST /tenders`, `POST /bids`, `POST /auctions`, `POST /auctions/{id}/bids`, وكل عمليات `zamakana` تقريبًا — راجع `_check_saas_limits` — كلها `403`/`PermissionDeniedError` لأي مستخدم). فئة "بيانات seed ناقصة" مش خطأ برمجي مشترك — لا علاقة له بأي دومين تاني حاليًا (باقي الدومينات تستخدم آلية SaaS مختلفة، `subscription.plan.features`). مؤكَّد حيًا لكل الثلاثة (استعلام مباشر `saas_service_catalog` = صفر صف بـ`code IN ('tenders','auctions','service_marketplace','zamakana')`) — راجع `batch2-audit-security-...`§3-أ (أول اثنين) و`batch5-audit-security-iot-translation-zamakana-admin-privacy-automation.md`§7 (`zamakana`، اكتُشف أثناء تحضير التحقق الحي، صفوف throwaway اتنضّفت بالكامل بعد التحقق). **✅ مُصلَح [2026-08-27]** — 4 صفوف أُدرجت مباشرة في `saas_service_catalog` (`tenders` id=74, `auctions` id=75, `service_marketplace` id=76, `zamakana` id=77، كلها `is_active=true`)، صفر تعديل كود. **تحقق حي:** `SaaSRepository.get_service_by_code()` رجعت صف حقيقي للأربعة (`id=74..77`) بدل `None` — الباج الموصوف (فشل عند خطوة البحث في الكتالوج نفسها) اختفى تمامًا؛ `can_access_service()` بترجع `False` لسه لتينانت1 (متوقَّع وصحيح — بلا اشتراك فعلي مسجَّل، مش نفس سبب الفشل القديم). **⚠️ اكتشاف جانبي أثناء التحقق، خارج نطاق البند الأصلي:** الكتالوج كان شبه فارغ تمامًا قبل هذا الإصلاح (3 صفوف فقط، اتنين منهم بادئة `TEST_REQSECTOR_SESSION_*` — بيانات throwaway مش seed حقيقي) — و**`transport`/`tourism_sports`/`social` بتستخدم نفس آلية `can_access_service(code)` بأكواد (`transport`/`tourism_sports`/`social`) كمان غير موجودة في الكتالوج**، بنفس الأثر بالضبط، **لكنها مش من ضمن الـ3 دومينات الموصوفة أصلًا في هذا البند ولا اتلمست هنا**. صفر إصلاح لها — يستاهل بند Backlog منفصل أو توسيع #28 نفسه لاحقًا. **✅ توسيع جزئي [2026-08-29]:** كود `"tourism"` (id=78) أُضيف كمان — مطلوب تحديدًا لفتح تحقق #37 على `tourism_sports.book_program` (كود مختلف عن `"tourism_sports"` الافتراضي). **`transport`/`social` لسه بلا كود، غير مُصلَحين.** |
| 29 | `finance-service-hold-funds-missing` [أُضيف 2026-08-25، بعد تجميد هذا الملف] | 🟢 محلي — **✅ مُصلَح ومُتحقَّق منه حيًا [2026-08-30]** | 1 دومين (`tenders_auctions`) | `tenders_auctions/service.py:347,406` يستدعي `finance.hold_funds(...)`/`finance.release_held_funds(...)` (معلَّمتان `# type: ignore[attr-defined]` في الكود نفسه) لكن `FinanceService` (321 سطر) **لا تحتوي غير `transfer()`** — `grep` شامل صفر نتيجة لأي منهما. يعطّل `place_bid`/`close_auction` (فرع الفائز) بالكامل لأي مستخدم بـ`AttributeError`/500، بصرف النظر عن التينانت. شبيه شكليًا بـ#18 (`finance-service-create-invoice-does-not-exist` في `transport`) لكن دالة مختلفة تمامًا ومحصور بدومين مختلف — بحث حي شامل أكَّد صفر استخدام للاسمين في أي دومين تالت. مؤكَّد كوديًا (`grep` مباشر) — راجع نفس التقرير §3-ب. **✅ الإصلاح المُطبَّق [2026-08-30] (جلسة `finance-service-hold-funds-missing`، بموافقة مستخدم صريحة على كل قرار تصميم):** عمود جديد `Wallet.held_balances` (JSONB، migration `042_add_held_balances_to_wallets.py`) + 3 دوال جديدة في `FinanceService`: `hold_funds`, `release_held_funds`, و`settle_held_funds` **ذرّية** (تحرير+تحويل الفائز في `begin_nested()` واحد، بدون المرور بـ`balances` المتاح — تقفل نافذة خطر كانت في التصميم الأولي المقترَح). + 4 إصلاحات إضافية اكتُشفت حيًا في الكولر نفسه أثناء التصميم (بدونها كان التنفيذ هيسيب تسريب مالي حقيقي حتى بعد سد الدالتين الناقصتين): `get_live_bids_for_auction` كانت ترتب `created_at DESC` (آخر مزايدة زمنيًا) بدل `bid_amount_mrusdt DESC` (الأعلى قيمة فعليًا) → **فائز غلط محتمل**؛ `close_auction` كانت تحرر حجز الفائز بس وتسيب كل المزايدات الخاسرة محجوزة للأبد → لوب يحرر حجز كل مزايدة غير فائزة؛ `idempotency_key` عشوائي (`uuid4()`) في استدعاء التحويل → مفتاح ثابت مُشتق من `auction_id`؛ `release_held_funds` بلا `idempotency_key` إطلاقًا → أُضيف. **تحقق حي كامل** (DB حقيقي، صفر mock، `tests/test_tenders_auctions_finance_hold_release_settle.py`): مزاد بـ3 مزايدين حقيقيين — الفائز الصحيح (أعلى قيمة فعليًا) اتحدد، الخاسرون استرجعوا حجزهم بالكامل، الفائز فضل عند نفس رصيده بعد الحجز طول الوقت (نافذة الخطر مقفولة)، `retry` حقيقي على `close_auction` أكَّد صفر تحويل/تحرير مزدوج. راجع `.claude/reports/finance-29-hold-funds-session-log.md` (كامل) و`PROGRESS_LOG.md` (بند #29). |
| 30 | `tenders-auctions-router-no-read-endpoints` [أُضيف 2026-08-25، بعد تجميد هذا الملف] | ⚪ ليس فئة باج (فجوة تصميم/تنفيذ، مش constructor mismatch) | 1 دومين (`tenders_auctions`) | `repository.py` يحتوي دوال قراءة كاملة (`list_tenders`, `get_tender`, `list_auctions`, `get_auction`, `list_bids_for_tender`, `get_live_bids_for_auction`) لكن **صفر `router` handler واحد يستدعيها** — الدومين غير قابل للاستكشاف عبر الـAPI أصلًا (يحتاج تخمين IDs تسلسلية لأي عملية). قرار منتجي/فجوة تنفيذ ناقصة، مش باج تقني عابر للدومينات. راجع نفس التقرير §2 |
| 31 | `frontend-service-url-prefix-mismatch` [أُضيف 2026-08-25، بعد تجميد هذا الملف] | 🔴 **مركزي — بند منتشر مستقل، نفس فئة #23 (`bleach-clean-none-crash`)** | **21 من 30 دومين فرونت إند قابل للتطبيق (تأكيد `grep`+تحقق حي مباشر)** | نمط عابر لغالبية دومينات الفرونت إند بالكامل — راجع القسم المخصَّص "🔴 بند منتشر مستقل — #31" مباشرة بعد هذا الجدول. صفر إصلاح الآن، توثيق موسَّع فقط. **✅ مُصلَح [2026-08-26، commit `6b4703a`]** — استبدال نصي ميكانيكي بحت في الـ21 ملف (614 إضافة/614 حذف بالضبط، صفر تغيير منطق). دليل حي مقتبس من `.claude/reports/frontend-url-prefix-comprehensive-audit-session-log.md` §12 (طلب حقيقي واحد لكل ملف ضد سيرفر حي، **بعد** التصحيح — النتائج `200`/`500`/`403` مش `404`، يعني المسار بقى بيوصل فعلًا): *"10/21 نجح بالكامل ... 9/21 وصل للراوتر الصحيح لكن كشف أعطال backend معروفة مسبقًا (يمدّ #25 لـ`GET /invitations/`، ويمدّ #28 لـ`social`/`tourism_sports`/`transport`/`tenders_auctions` بالإضافة لـ`zamakana`) ... 2/21 كشفوا أعطال backend جديدة غير موثَّقة قبل كده — سُجِّلوا كـ#46 و#47"* — تحقق `tsc --noEmit` قبل/بعد أظهر **byte-identical output** (صفر خطأ TypeScript جديد). **الأعطال الـ6 المكشوفة اتصلحت لاحقًا في commit `f17636c`** (راجع #25، #27، #46، #47 أعلاه). |
| 32 | `arbitration-syndicates-nominate-candidate-wrong-kwarg` [أُضيف 2026-08-25، بعد تجميد هذا الملف] | 🟡 غير مؤكَّد الانتشار (نفس فئة `duplicate-kwarg-audit`/#2، محلي لحد ما يُفحص) | 1 مؤكَّد (`arbitration_syndicates`) | `service.py:479-486` (`nominate_candidate`) يستدعي `self.repo.create_candidate(..., candidate_user_id=user_id, ...)` لكن عمود الموديل `ElectionCandidate` الفعلي اسمه `user_id`، مش `candidate_user_id` — `TypeError` فوري (`ElectionCandidate(**kwargs)` بيرفض kwarg غير موجود كعمود)، **قبل أي `db.add()`/`commit()`** — يمنع `POST /elections/{id}/candidates` بالكامل لأي مستخدم، بصرف النظر عن التينانت. مؤكَّد حيًا (هجوم IDOR بعد إصلاح `arbitration_syndicates` وصل لنفس العطل — صفر كتابة، `SELECT` مستقل = 0) — راجع `.claude/reports/batch2-audit-security-service-marketplace-tenders-auctions-arbitration-syndicates.md` §13.3. لم يُفحَص انتشار نفس نمط `candidate_user_id`/تسمية kwarg مشابهة في دومين تاني. **✅ مُصلَح [2026-08-29]** — `candidate_user_id=` → `user_id=`. **grep تأكيدي [2026-08-29]:** `candidate_user_id` ظهرت مرة واحدة بس في كل السورس (نفس الموضع) — **صفر انتشار مؤكَّد**. دليل حي (قبل/بعد): *"CONFIRMED bug (old code): TypeError as expected: 'candidate_user_id' is an invalid keyword argument for ElectionCandidate"* ثم *"create_candidate() succeeded: id=1, user_id=991, status=APPROVED"* — تحقق `SELECT` مستقل مطابق. |
| 33 | `tenders-auctions-naive-vs-aware-datetime` [أُضيف 2026-08-25، بعد تجميد هذا الملف] | 🟢 محلي | 1 مؤكَّد (`tenders_auctions`) | `service.py:132` (`submit_bid`) و`service.py:316` (`place_bid`) يقارنان `datetime.utcnow()` (naive) مباشرة ضد أعمدة `DateTime(timezone=True)` (aware — `tender.submission_deadline`, `auction.start_time`/`end_time`) — `TypeError: can't compare offset-naive and offset-aware datetimes` فوري. **يمنع المسار الشرعي الكامل لكلا الـendpoint** (الهجوم عبر IDOR غير متأثر — بيتردّ عند فحص التينانت **قبل** الوصول لهذا السطر أصلًا). مؤكَّد حيًا أثناء التحقق الجزئي in-process لإصلاح `tenders_auctions` IDOR (نفس الـtraceback ظهر مرتين، مرة لكل endpoint) — راجع `.claude/reports/batch2-audit-security-service-marketplace-tenders-auctions-arbitration-syndicates.md` §17.3. لم يُفحَص انتشار نفس نمط `datetime.utcnow()` naive ضد عمود `timezone=True` في دومين تاني. **✅ مُصلَح [2026-08-29]** — الموضعين: `datetime.now(timezone.utc)` بدل `datetime.utcnow()`. دليل حي للموضعين (قبل/بعد، بيانات throwaway حقيقية عبر الـORM): tender — *"CONFIRMED bug (old code): TypeError as expected: can't compare offset-naive and offset-aware datetimes"* ثم *"FIXED comparison result (deadline is 5 days out, expect False): False"*؛ نفس النمط بالحرف لـauction (`start_time`/`end_time`). |
| 34 | `manufacturing-facilities-entity-id-missing` [أُضيف 2026-08-25، بعد تجميد هذا الملف] | 🟢 محلي — **✅ مُصلَح فعليًا في نفس جلسة الاكتشاف** | 1 دومين (`manufacturing`) | `ManufacturingFacilityCreate`/`service.create_facility` لم يكونا يمرران `entity_id` إطلاقًا رغم `NOT NULL` على `manufacturing_facilities.entity_id` — `IntegrityError` فوري لأي طلب `POST /manufacturing/facilities`، لأي تينانت. مؤكَّد حيًا (`INSERT` فشل بـ`NotNullViolationError`) — راجع `.claude/reports/batch3-audit-security-transport-logistics-manufacturing-tourism-sports.md` §3.1. **الإصلاح:** `entity_id` بقى حقل إجباري في `ManufacturingFacilityCreate` (قرار منتجي صريح من المستخدم: مفهوم مطلوب فعليًا زي `transport`/`tourism_sports`، مش عمود زايد) + تمريره في `service.create_facility`. تحقق حي بعد الإصلاح: `POST /manufacturing/facilities` نجح (`201`)، `SELECT` مستقل أكَّد `entity_id` مكتوب صح |
| 35 | `manufacturing-repository-hardcoded-tenant-1-fallback` [أُضيف 2026-08-25، بعد تجميد هذا الملف] | 🟡 **غير مؤكَّد الانتشار خارج `manufacturing`** — **الموضعان المعروفان ✅ مُصلَحان فعليًا في نفس جلسة الاكتشاف** | 2 موضع مؤكَّد، 1 دومين (`manufacturing`) | `repository.py` (`update_batch_status`، `update_spare_part_stock`) كان فيهم fetch داخلي بـ`tenant_id=1` هاردكودد حرفيًا (تعليق "سيتم تمريره من Service" وصف نية لم تُنفَّذ). `update_spare_part_stock` كان **يتفاعل فعليًا مع ثغرة X-Tenant-ID IDOR** — مؤكَّد حيًا اتجاهين: استغلال ناجح لموارد تينانت1 تحديدًا (`stock_quantity` تغيَّر فعليًا من `10` إلى `510` عبر هيدر مزوَّر)، وعطل وظيفي بحت (`500`) لأي تينانت آخر. راجع `.claude/reports/batch3-audit-security-transport-logistics-manufacturing-tourism-sports.md` §3.2/§6.2. **الإصلاح:** الدالتان بقيتا تاخدان `tenant_id` كوسيط حقيقي (مُمرَّر من `service.py`، نابع من `current_user.tenant_id` بعد إصلاح IDOR نفسه)، `update_batch_status` كمان بقى فيها فحص تينانت في الـ`UPDATE` نفسه (دفاع بعمق، عبر join لـ`ProductBlueprint.tenant_id`). تحقق حي بعد الإصلاح: نفس الاتجاهين اللي كانا ناجحين/معطوبين بقوا `404` نظيف. **لسه محتاج `grep` شامل عبر باقي الـ34 دومين للتأكد من عدم وجود نفس نمط "`# ... سيتم تمريره من Service` + هاردكود فعلي" — لم يُنفَّذ في هذه الجلسة (تُرك لجلسة تصنيف الأولويات القادمة بتوجيه صريح من المستخدم)** |
| 36 | `manufacturing-schedule-maintenance-missing-commit` [أُضيف 2026-08-25، بعد تجميد هذا الملف] | 🟢 محلي (مبدئيًا) — **✅ مُصلَح فعليًا في نفس جلسة الاكتشاف** | 1 دومين (`manufacturing`) | `schedule_maintenance` (service+repository) كان يستخدم `flush()` بدل `commit()` في كامل مسار الاستدعاء (لا الـservice ولا الـrouter كانا يستدعيان `commit()` إطلاقًا) — الكتابة كانت **تضيع صامتة** عند إغلاق جلسة DB (`get_db` بيعمل `close()` بلا commit تلقائي)، مع رد `200` كاذب يوحي بالنجاح. مؤكَّد حيًا **قبل** الإصلاح: `SELECT` مستقل بعد استدعاء ناجح ظاهريًا أظهر الصف بلا أي تغيير. راجع نفس التقرير §3.2. **الإصلاح:** إضافة `await self.db.commit()` صريح في `service.schedule_maintenance` بعد نجاح فحص الملكية والتحديث. تحقق حي بعد الإصلاح: نفس الاستدعاء الشرعي، `SELECT` مستقل أظهر `status='SCHEDULED'`/`maintenance_scheduled_at` مكتوبين فعليًا |
| 37 | `finance-transfer-payment-tx-hash-broken` [أُضيف 2026-08-25 — أول مرة يُدرَج بند رسمي بهذا الاسم في هذا الملف؛ الأصل موثَّق مسبقًا في `technical-pattern-sweep-session-log.md`] | 🔴 **مركزي — بند منتشر مستقل، نفس فئة #23/#31** | **4 مواضع حية مؤكَّدة عبر 3 دومينات مستقلة (`realestate`, `insurance`, `transport.pay_delivery`، + الآن `tourism_sports.book_program`)، ~9 مواضع إضافية "تسمية مربكة بلا كراش" عبر 6 دومينات أخرى (غير مؤكَّدة حرجة)** | `finance.transfer()` بترجّع كائن `Transaction` كامل (SQLAlchemy model)، مش نص `tx_hash`. أغلب الاستدعاءات بتخزّن الناتج في متغيّر اسمه `tx_hash` بس من غير استخدامه فعليًا في DB لاحقًا (كود مربك، غير مُكسِّر). **لكن 4 مواضع بتخزّن الكائن مباشرة في عمود `String`** — `DataError`/`IntegrityError` مضمون: `realestate`، `insurance` (مكتشَفان أصلًا في `technical-pattern-sweep-session-log.md`)، `transport.pay_delivery` (`service.py:512`، `DeliveryTask.payment_tx_hash`، مؤكَّد كوديًا فقط — الدومين معطَّل بنيويًا حاليًا، راجع بند #39)، و**رابع موضع جديد مؤكَّد حيًا في هذه الجلسة: `tourism_sports.book_program`** (`ProgramParticipant.payment_tx_hash`) — تحقق حي: `POST /tourism-sports/programs/{id}/book` رجع `500 DataError: invalid input for query argument $8: expected str, got Transaction` بعد نجاح الخصم من محفظة المُرسِل ونجاح فحص IDOR (الـ`begin_nested()` عمل rollback كامل، صفر أثر جانبي مالي). راجع `.claude/reports/batch3-audit-security-transport-logistics-manufacturing-tourism-sports.md` §6.3. **صفر إصلاح لأي موضع من الأربعة حتى الآن — بانتظار جلسة مركزية واحدة تستخرج `.tx_hash` (أو الحقل المناسب) من الكائن قبل التخزين، عبر كل المواضع الأربعة معًا**. **🔗 Cross-reference [2026-08-28]: #37 متشابك كوديًا (مش مجرد تشابه فئة) مع #39/#40/#41 — كل موضع من الأربعة محجوب بباج منفصل تمامًا *قبل* ما يوصل لسطر `tx_hash` نفسه: `realestate.buy_fractional_ownership` محجوب بـ#40 (`execute_agent_action` توقيع قديم، `service.py:239`، قبل سطر #37 بـ23 سطر)، `insurance.review_claim` محجوب بـ#41 (`issuer_entity_id`/`reviewer_id` مقارنة خاطئة، `service.py:431`، قبل سطر #37 بـ44 سطر)، `transport.pay_delivery` محجوب بـ#39 (جداول `transport` غير موجودة). يعني: إصلاح #37 وحده (استخراج `.tx_hash`) **لا يكفي لتحقق حي end-to-end** عبر أي من الثلاثة دول إلا بعد حل #39/#40/#41 المقابل له — راجع `.claude/reports/constructor-mismatch-backlog-37-blockers-found.md` للتفصيل الكامل موضع بموضع.** **✅ الأربعة مواضع مُصلَحة كودًا [2026-08-28/29]** (`.tx_hash`/`payout_tx.tx_hash` بدل الكائن الكامل، بلا `monkeypatch` على أي فحص صلاحيات/أمان — ممنوع صراحة): `realestate` و`insurance` تحقُّق على مستوى الـrepository مباشرة (بمعزل عن #40/#41، موثَّق بوضوح إنه مش end-to-end)؛ `tourism_sports.book_program` **تحقُّق end-to-end كامل عبر الـservice method الحقيقية** (بعد توسيع #28 بكود `"tourism"` + سلسلة اشتراك SaaS حقيقية — دليل حي: `program_participants.payment_tx_hash='TX-0FC913F7A97E'` == `transactions.tx_hash` بالحرف)؛ `transport.pay_delivery` فحص سورس فقط (جداول غير موجودة، #39). تفاصيل كاملة: `.claude/reports/constructor-mismatch-backlog-37-insurance-result.md`، `.claude/reports/constructor-mismatch-backlog-37-tourism-transport-result.md`. **اكتشاف جانبي جديد أثناء تحقق `tourism_sports` (صفر لمس، خارج نطاق #37):** `invoicing-generate-invoice-number-count-based-collision` — راجع `PROGRESS_LOG.md`. |
| 38 | `tourism-sports-player-profile-wrong-column-filter` [أُضيف 2026-08-25، بعد تجميد هذا الملف] | 🟡 غير مؤكَّد الانتشار (لم يُفحَص خارج `tourism_sports`) | 1 مؤكَّد (`tourism_sports`) | `place_transfer_bid` (`service.py:402`) يستدعي `self.repo.get_player_profile(data["player_id"])`، لكن `get_player_profile(user_id)` بتفلتر بعمود `PlayerProfile.user_id`، بينما `data["player_id"]` (حسب وصف `TransferBidCreate.player_id` — "معرف اللاعب") المقصود بيه `PlayerProfile.id` (المفتاح الأساسي)، **مش** `user_id`. أي استدعاء حقيقي بمعرف ملف لاعب صحيح شبه مضمون يفشل بـ`NotFoundError`، إلا لو تصادف رقميًا `PlayerProfile.id == PlayerProfile.user_id` لنفس الصف بالصدفة. **غير مؤكَّد حيًا** (اكتُشف أثناء قراءة الكود فقط، خارج نطاق وقت جلسة `batch3-audit-security-...`) — راجع نفس التقرير §4. **✅ مُصلَح ومُتحقَّق منه حيًا [2026-08-29]** — method جديدة `get_player_profile_by_id(profile_id)` (تفلتر بـ`PlayerProfile.id`) أُضيفت، `place_transfer_bid` بقت تناديها بدل `get_player_profile` (اللي فضلت زي ما هي لموضعها الشرعي التاني، `service.py:365`). دليل حي (بيانات throwaway بـ`id != user_id` متعمَّد): *"OLD call get_player_profile(profile_id=4) -> None (expected None: 4 is not anyone's user_id)"* ثم *"NEW call get_player_profile_by_id(profile_id=4) -> id=4, user_id=998"* — يثبت الفرضية الموصوفة بالحرف (كانت هترجع `NotFoundError` لأي `profile_id` شرعي فعليًا). |
| 39 | `transport-domain-tables-never-migrated` [أُضيف 2026-08-25، بعد تجميد هذا الملف] | 🔴 **بند مستقل بالكامل — ليس فئة `constructor-mismatch` (لا يوجد تعارض schema↔model، بل غياب تام لأي migration)، قرار تصنيف نهائي متروك لجلسة تصنيف الأولويات القادمة (بتوجيه صريح من المستخدم)** | 1 دومين (`transport`)، **لم يُفحَص انتشار نفس النمط (موديل مُعرَّف بلا migration) عبر باقي الـ34 دومين — بتوجيه صريح: لا جرد موسَّع الآن** | 7 جداول أساسية لدومين `transport` (`fleets`, `vehicles`, `transport_hubs`, `transport_routes`, `transport_trips`, `trip_bookings`, `delivery_tasks`) **غير موجودة إطلاقًا** في schema الفعلي — تأكيد مباشر `pg_tables` + `grep` شامل على `migrations/versions/` (41 ملف) لأي إشارة لهذه الجداول = صفر نتيجة + `alembic current` يطابق `head` (لا migrations معلَّقة). يعطّل الدومين بالكامل (16/16 endpoint) على مستوى DB، بمعزل تام عن أي ثغرة IDOR. راجع `.claude/reports/batch3-audit-security-transport-logistics-manufacturing-tourism-sports.md` §1.1. **الحالة:** توثيق فقط — تم تنفيذ إصلاح IDOR كودي احترازي على الـ16 endpoint (نفس نمط `get_my_licenses` من الدفعة 2: يمنع الثغرة من الظهور فور تفعيل أي migration مستقبلية) **بلا أي محاولة تحقق حي** (مستحيل بدون الجداول) — القرار المعماري (هل الدومين يستحق التفعيل الفعلي؟) مؤجَّل بالكامل |

**ملاحظة [2026-08-25، استكمال جلسة `batch2-audit-security-...`]:** البندان #32 و#33 أُضيفا بعد تنفيذ إصلاحات `arbitration_syndicates`/`tenders_auctions` نفسها (اكتشافات جانبية أثناء التحقق الحي/الجزئي، مش جزء من الجرد الأصلي) — بتوجيه صريح من المستخدم.

| 40 | `realestate-ai-agents-execute-agent-action-stale-signature` [أُضيف 2026-08-26، بعد تجميد هذا الملف] | 🟡 **امتداد محتمل لـ#16 (`ai-agents-execute-agent-action-wrong-kwarg`) — لم يُفحَص رسميًا كموضع إضافي لنفس البند، لكن نفس الشكل بالضبط (kwarg غير مقبول)** | 1 موضع مؤكَّد حيًا (`realestate.buy_fractional_ownership`) | 🔴 **تحذير عملية مهم مطلوب تسجيله صراحة:** جلسة `ai-agents-idor-fix` (2026-08-24) غيّرت توقيع `AIAgentsService.execute_agent_action()` (حذف معامل `tenant_id`، أضافت `idempotency_key` إجباري) كجزء من إصلاح IDOR الأساسي في `ai_agents/router.py` نفسه — لكن **لم تُراجَع بقية المستدعين خارج `ai_agents`**. `realestate/service.py:232` لسه بينادي بالتوقيع القديم (`tenant_id=..., `بلا `idempotency_key=`) → `TypeError` مضمون، يمنع `POST /realestate/units/{id}/buy` بالكامل (endpoint الشراء الوحيد في الدومين) — مؤكَّد حيًا أثناء `batch4-audit-security-realestate-insurance-health.md` §4.3، صفر أثر جانبي (rollback كامل قبل أي `commit`). **الدرس العام لأي جلسة إصلاح توقيع دالة مستقبلية: `grep` شامل على كل المستدعين عبر كل الدومينات، مش فقط الدومين اللي بيحصل فيه الإصلاح نفسه** — لم يُنفَّذ هذا الـ`grep` بعد لتأكيد/نفي مواضع إضافية مشابهة لـ`execute_agent_action` تحديدًا (بمعزل عن #16 الأوسع). **✅ مُصلَح ومُتحقَّق منه حيًا [2026-08-29]** — حذف `tenant_id=` + إضافة `idempotency_key=f"REALESTATE-FRAC-T{tenant_id}-{...}"` (نمط `PREFIX-T{tenant_id}-{unique_id}` القياسي من جلسة #16 الأصلية). **grep تأكيدي [2026-08-29]:** `grep` شامل لكل استدعاءات `execute_agent_action(` بمعامل `tenant_id=` عبر كل الدومينات — **صفر نتيجة إضافية**، `realestate` كان الموضع الوحيد المتبقي فعليًا. دليل حي (قبل/بعد، وكيل throwaway حقيقي): *"CONFIRMED bug (old code): TypeError as expected: AIAgentsService.execute_agent_action() got an unexpected keyword argument 'tenant_id'"* ثم *"execute_agent_action() succeeded: {'status': 'COMPLETED', ...}"* مع سجل `audit_logs` حقيقي مطابق. |
| 41 | `insurance-review-claim-issuer-entity-id-reviewer-id-mismatch` [أُضيف 2026-08-26، بعد تجميد هذا الملف] | ✅ **مُصلَح ومُتحقَّق منه حيًا [2026-08-29] — كان `broken-closed` (الميزة معطّلة بالكامل لكل الشرعيين)، مش `broken-open` (ثغرة صلاحية مفتوحة)** | 1 دومين (`insurance`) | `review_claim` (`service.py:431`، قبل الإصلاح) الفحص الثانوي المفروض يمثّل طبقة حماية حقيقية كان يقارن `policy.issuer_entity_id` (مفتاح أساسي لجدول `sovereign_entities_v2`) مقابل `reviewer_id` (مفتاح أساسي لجدول `users`) — **مساحتا معرِّفات مختلفتان تمامًا**. راجع `batch4-audit-security-realestate-insurance-health.md` §2.1. **🔴 تحقيق حي كامل [2026-08-29]، جلسة `insurance-41-review-claim-permission-check`، أثبت التصنيف الدقيق بشكل قاطع (مش مجرد "غير مؤكَّد" كما كان مسجَّلًا هنا سابقًا):** بُنيَ سيناريو حي كامل (بوليصة→اشتراك→مطالبة+`EntityMembership` حقيقية) واستُدعيت `review_claim()` الأصلية غير المُعدَّلة بثلاث حالات — **حتى "أقرب تعريف ممكن لمراجع شرعي" (superuser عالمي + `EntityMembership.role=OWNER` حقيقي على نفس الكيان) اترفض بـ`PermissionDeniedError`، بنفس الرسالة بالحرف اللي اترفض بيها مستخدم عشوائي مالوش أي علاقة.** يعني: **الفحص لم يكن "فيه ثغرة تسمح لغير المخوَّل" — كان معطّلًا بالكامل، يمنع حتى المخوَّل الحقيقي.** ده الفرق الجوهري بين `broken-open` (كان القلق الأصلي هنا) و`broken-closed` (الواقع الفعلي المؤكَّد). **الإصلاح المُطبَّق:** استبدال المقارنة بفحص `EntityMembership` حقيقي (`entity_type="SOVEREIGN_ENTITY"`, `role in [OWNER, EXECUTIVE_DIRECTOR]`) بنفس نمط `_is_authorized_representative`/`deposit_to_entity_wallet` في `sovereign_entities/service.py` بالحرف — **بموافقة مستخدم صريحة على الخيار الأضيق للأدوار**. + إزالة بوابة `get_current_superuser` من الراوتر (دور منصة عالمي غير مرتبط منطقيًا بعضوية الكيان، كان سيجعل الفحص الجديد الصحيح غير قابل للتفعيل عمليًا لو بقيت) — استُبدلت بـ`get_current_active_user`، بموافقة مستخدم صريحة أيضًا، ونفس نمط بقية `sovereign_entities` (لا يوجد فيها أي بوابة `system_role` مكدَّسة فوق فحص العضوية). **تحقق حي كامل بعد الإصلاح لكلا المسارين:** عضو `EntityMembership` حقيقي بدور `OWNER` → **نجح فعليًا** (claim → `REJECTED`، تأكيد `SELECT` مستقل)؛ عضو `EntityMembership` حقيقي لكن بدور غير كافٍ (`REPRESENTATIVE`) → **لسه بيترفض بـ403، لكن الآن بوعي حقيقي بالدور (`role-aware`)** — يثبت إن الإصلاح مش "أي عضوية كافية" (بالضبط التحذير الساذج المسجَّل هنا سابقًا لم يتحقق: الإصلاح المُطبَّق ضيَّق الصلاحية أكتر من الفحص المكسور القديم من ناحية دلالية، رغم إنه فتحها عمليًا لأول مرة لأي مستخدم). Commit `99253e3`. راجع `.claude/reports/insurance-41-review-claim-permission-check-session-log.md` للتفصيل الكامل خطوة بخطوة (اقتباسات كود + نتائج A/B/C حية + جدول قبل/بعد). |
| 42 | `health-create-prescription-zero-ownership-check` [أُضيف 2026-08-26، بعد تجميد هذا الملف] | ⚪ **ليس فئة باج قابلة للتصنيف الآلي — حساسية طبية، ممنوع اللمس بلا نقاش منفصل صراحةً** | 1 دومين (`health`) | `POST /health/prescriptions` — صفر فحص ملكية/تينانت على `consultation_id` (`repo.get_consultation` بلا فلتر)، و`patient_id` (المريض المنسوبة له الوصفة) غير موجود أصلًا في `PrescriptionCreate` schema (`data.get("patient_id")` دايمًا `None` → `IntegrityError` مضمون حاليًا، `Prescription.patient_id` عمود `NOT NULL`). راجع `batch4-audit-security-realestate-insurance-health.md` §7.1 بند 9. **لو الـschema اتصلحت بحسن نية (حتى كإصلاح وظيفي بحت بلا أي نية أمنية) بلا مراجعة أمنية مصاحبة، ثغرة تلفيق سجل دوائي كامل لمريض حقيقي (تشخيص + أدوية + ملاحظات طبيب) تتفعّل فورًا لأي `active_user` عادي.** **🔴🔴🔴 توجيه صريح من المستخدم: لا تُلمَس هذه الدالة/الـschema إطلاقًا (لا إصلاح وظيفي ولا أمني) بلا جلسة نقاش منفصلة مخصَّصة — الحساسية الطبية أعلى من المعتاد، القرار يحتاج تدقيق أعمق من نمط IDOR القياسي.** |

| 43 | `frontend-service-export-import-name-mismatch` [أُضيف 2026-08-26، جلسة `batch5-audit-security-...`] | 🟡 غير مؤكَّد الانتشار (نفس فئة #32/#38 — مؤكَّد في أكثر من موضع لكن بلا `grep` شامل عبر كل الدومينات) | 2 مؤكَّدة (`realestate.service.ts`، `batch4-audit-security-...`§6 بند 6؛ `iot.service.ts`، `batch5-audit-security-...`§1.4) | ملف `services/*.ts` يُصدِّر الكائن باسم PascalCase (مثال `IoTService`) لكن كل المكوّنات المستهلِكة تستورده باسم camelCase (`iotService`) — `TS2724` قاطع (عضو تصدير غير موجود)، موثَّق فعليًا في `eppne-web/link-fix-verification.txt`. **مستقل تمامًا عن #31 (بادئة المسار المضاعفة)** — حتى لو اتصلح #31، الاستيراد نفسه لسه هيفشل في build/runtime. لم يُنفَّذ `grep` شامل (`export const \w+Service` مقابل `import { \w+ }`) عبر باقي ملفات `services/*.ts` لتأكيد/نفي انتشاره أوسع من الموضعين المعروفين. **✅ مُصلَح جزئيًا + تصحيح دقيق على النطاق [2026-08-29]:** `grep` شامل حقيقي (32 ملف `services/*.ts` × كل الاستيرادات المستهلِكة، مش عيّنة) طلع النتيجة التالية: **`iot` (4 ملفات: `AssetCard`, `AssetsManager`, `CarbonCreditPanel`, `IoTDashboardStats`) و`translation` (4 ملفات: `BatchTranslator`, `ChatTranslator`, `LanguageSelector`, `TextTranslator`) هما الوحيدان بنمط camelCase-object بالحرف — 8 ملفات بالظبط، صفر انتشار إضافي.** الاتنين اتصلحوا (`iotService`→`IoTService`، `translationService`→`TranslationService`، replace شامل في كل ملف). **⚠️ تصحيح على `realestate` تحديدًا: مش نفس الفئة إطلاقًا** — `services/realestate.ts` بيصدِّر بس `RealEstateService` (كائن واحد)، لكن `hooks/realestate/*.ts` (4 ملفات: `useMyOwnerships`, `useProperties`, `usePropertyOwnerships`, `useTokenization`) بتستورد **دوال مباشرة غير موجودة إطلاقًا** (`getProperties`, `getProperty`, `createProperty`, `updateProperty`, `deleteProperty`, `getPropertyOwnerships`, `getAssetTokenization`, `createTokenization`, `buyFractionalShare`, `getMyOwnerships`) — مش مجرد اختلاف حالة أحرف، أسماء مختلفة تمامًا عن methods الكائن الفعلية (`createPropertyUnit`, `listUnitsForSale`, `buyFraction`, `tokenizeAsset`, ...). **بند منفصل تمامًا، أعمق بكتير من #43 — راجع `PROGRESS_LOG.md` لبند Backlog جديد مخصَّص.** **✅ تحديث [2026-08-31]: بند الـrealestate اتقفل بالكامل في الباك إند** — جلسة `realestate-hooks-layer-design-decision` بنت كل الـ8 وصلات الناقصة (migration + service + router)، صفر تعديل فرونت إند لسه مطلوب لاحقًا لربط الاستيراد الفعلي. راجع `PROGRESS_LOG.md` بند `realestate-hooks-layer-design-decision` [2026-08-31] و`.claude/reports/realestate-design-decision-session-log.md` للتفصيل الكامل. **تحقق `tsc --noEmit` بعد إصلاح الـ8 ملفات:** صفر `TS2304 Cannot find name` متبقي للاتنين، `LanguageSelector.tsx` بقت نضيفة 100%؛ 7 ملفات تانية كشفوا أعطال أعمق (method/type mismatches داخل الكائن الصحيح نفسه، زي `getAssets` مقابل `getAsset` الحقيقية) — **موثَّقة كاكتشاف جانبي منفصل في `PROGRESS_LOG.md`، صفر إصلاح.** |
| 44 | `permission-check-passed-id-instead-of-user-object` [أُضيف 2026-08-26، جلسة `batch5-audit-security-...`] | 🟡 غير مؤكَّد الانتشار | 1 مؤكَّد (`privacy.is_privacy_officer`، موضعان: `router.py:237` و`service.py:124`) — **✅ مُصلَح فعليًا في نفس جلسة الاكتشاف** | دالة صلاحيات بتوقيع `async def is_privacy_officer(user: User) -> bool` (`core/security.py:189`، بتقرأ `getattr(user, "system_role", None)`) كانت بتتنادى بـ`current_user.id` (`int`) بدل `current_user` (الكائن) في كلا الموضعين — `getattr(<int>, "system_role", None)` يرجع `None` دايمًا → الدالة ترجع `False` لأي مستخدم، **حتى `SUPER_ADMIN` حقيقي**. self-DoS كامل لقسم إدارة طلبات محو GDPR بالكامل (`GET /admin/erasure/pending`, `POST /admin/erasure/{id}/process` مقفولان بـ403 دائمًا). **الإصلاح المُطبَّق:** الموضعان بقيا يمرّران `current_user`/`admin_user: User` كامل بدل `.id`، مع تحديث توقيع `PrivacyService.process_erasure_request` (`admin_id: int` → `admin_user: User`) + `import User` في `service.py`. **تحقق حي بعد الإصلاح:** `GET /admin/erasure/pending` بتوكن `SUPER_ADMIN` حقيقي → `200` (بدل 403 دائم)؛ دورة كاملة `POST /erasure/request` → `POST /admin/erasure/{id}/process?approved=false` → `200 REJECTED` (مؤكَّد بـ`SELECT` مستقل: `status='REJECTED'`, `admin_notes` مطابق). راجع `batch5-audit-security-iot-translation-zamakana-admin-privacy-automation.md`§5.1. **لم يُفحَص انتشار نفس النمط** (دالة صلاحيات تتوقع `User` كامل، تتنادى بـ`.id` بس) في دومينات تانية — يستاهل `grep` شامل مستقبلي |
| 45 | `global-unique-column-conflicts-with-tenant-scoped-query` [أُضيف 2026-08-26، جلسة `batch5-audit-security-...`] | 🟡 غير مؤكَّد الانتشار | 1 مؤكَّد (`translation.TranslationCache.text_hash`, `models.py:16`) | عمود DB بقيد `unique=True` **عالمي** (بلا `tenant_id` كجزء من الـconstraint)، بينما منطق التطبيق (`repository.py:13-20`, `get_cache_by_hash`) بيفلتر بـ`tenant_id`+هذا العمود معًا. لو تينانت A ترجم نص X (صف موجود بـ`tenant_id=A`)، وتينانت B حاول ترجمة **نفس النص بالحرف** — الاستعلام المفلتَر بتينانت B يرجع `None` (الصف موجود لكن لتينانت تاني)، فالتطبيق يكمل لاستدعاء API ترجمة حقيقي ثم `INSERT` صف جديد بنفس `text_hash` → `IntegrityError` (انتهاك unique) → `500`. **DoS وظيفي محتمل لنصوص شائعة بين تينانتين مختلفين، ليس تسريب بيانات.** غير مختبَر حيًا (يحتاج تينانتين + نص مطابق حرفيًا). راجع `batch5-audit-security-iot-translation-zamakana-admin-privacy-automation.md`§2.1. **صفر إصلاح — لم يُفحَص انتشار نفس النمط** (عمود `unique=True` بلا `tenant_id` في الـconstraint، مستخدَم باستعلام مفلتَر بـtenant) في دومينات تانية. **✅ مُصلَح ومُتحقَّق منه حيًا [2026-08-29]، migration `040_fix_translation_cache_global_unique_text_hash`** — إسقاط `unique=True` من `text_hash` وحده، إضافة composite `UNIQUE(tenant_id, text_hash)` (`uq_translation_cache_tenant_text_hash`) بدلًا منه؛ index عادي غير-unique أُبقي على `text_hash` وحده لأداء البحث. **تحقق حي قبل الـmigration (يثبت التعارض فعليًا):** *"tenant 16, before: get_cache_by_hash(16, same hash) -> None ... CONFIRMED bug: IntegrityError on tenant 16's commit -- UniqueViolationError: duplicate key value violates unique constraint 'ix_translation_cache_text_hash'"* (تينانتين حقيقيين `1`/`16`، نفس النص بالحرف). **تحقق حي بعد الـmigration (نفس السيناريو بالضبط):** تينانت16 نجح في حفظ نفس النص بلا أي `IntegrityError`، و`ASSERTION PASSED: two independent rows, same text_hash, different id, correct tenant isolation` — صفين مستقلين، نفس `text_hash`، `id` مختلف، عزل تينانت صحيح. |
| 46 | `insurance-policies-response-validation-terms-and-conditions-null` [أُضيف 2026-08-26، جلسة تحقق حي لبند #31] | 🟢 محلي — **✅ مُصلَح [2026-08-26]** | 1 دومين (`insurance`) | `insurance/schemas.py`: `PolicyResponse.terms_and_conditions: Dict[str, Any]` (بلا `Optional`، `default_factory=dict`) بينما `insurance/models.py:61` (`terms_and_conditions = Column(JSONB, default=dict)`) عمود قابل لـ`NULL` فعليًا في DB (صف/صفوف قديمة). `default_factory` لا يحمي من `NULL` صريح قادم عبر `from_attributes=True` — `500 ResponseValidationError` لأي `GET /insurance/policies` يشمل صفًا متأثرًا. اكتُشف أثناء التحقق الحي لبند #31 (`frontend-service-url-prefix-mismatch`) — أول مرة يوصل طلب حقيقي لهذا الـendpoint أصلًا. **✅ الإصلاح المُطبَّق:** الحقل بقى `Optional[Dict[str, Any]] = Field(default=None, ...)` — يعكس واقع العمود مباشرة، بلا حاجة لـvalidator. **تحقق حي بعد الإصلاح:** `GET /insurance/policies` → `200`، الصف القديم (`terms_and_conditions:{}`) رجع بلا كراش. راجع `.claude/reports/frontend-url-prefix-comprehensive-audit-session-log.md` §12 صف 6، §13 (بند جديد #46)، §18 بند 5، §20 |
| 47 | `translation-redis-client-singleton-called-as-factory` [أُضيف 2026-08-26، جلسة تحقق حي لبند #31] | 🟢 محلي — **✅ مُصلَح [2026-08-26]** | 1 دومين (`translation`) | `translation/service.py:23` يستورد الكائن الجاهز (singleton) `redis_client` بلقب مضلِّل `as get_redis_client`، ثم `service.py:30` يناديه كأنه دالة مصنع: `self.redis = get_redis_client()` — `RedisClientWrapper` لا يعرِّف `__call__`، فـ`500 TypeError: 'RedisClientWrapper' object is not callable` لأي استدعاء يلمس `self.redis` (كل مسارات الكاش/idempotency في الدومين). **مختلف جذريًا عن #7** (`redis-client-wrapper-missing-methods` — دوال ناقصة فعليًا في الكلاس) — هنا الكلاس سليم، المشكلة سوء استخدام/تسمية alias فقط. اكتُشف أثناء التحقق الحي لبند #31. **✅ الإصلاح المُطبَّق:** حذف الـalias، استيراد `redis_client` مباشرة، استخدامه ككائن بلا `()`. **تحقق حي بعد الإصلاح:** `GET /translation/languages` → `200` (بدل `500`). راجع `.claude/reports/frontend-url-prefix-comprehensive-audit-session-log.md` §12 صف 17، §13 (بند جديد #47)، §18 بند 1، §20 |
| 48 | `iot-translation-service-method-mismatches-post-backlog43-fix` [أُضيف 2026-08-29، بعد تجميد هذا الملف — بُنِّد رسميًا بناءً على طلب صريح] | 🟡 غير مؤكَّد الانتشار خارج الملفين (لم يُفحَص باقي ملفات `services/*.ts`) | 7 ملفات مؤكَّدة (`components/iot/AssetsManager.tsx`, `CarbonCreditPanel.tsx`, `IoTDashboardStats.tsx` + `components/translation/BatchTranslator.tsx`, `ChatTranslator.tsx`, `TextTranslator.tsx`؛ `LanguageSelector.tsx` بقت نضيفة بعد #43) | اكتُشف فورًا بعد إصلاح #43 (rename `iotService`→`IoTService`، `translationService`→`TranslationService`) — قبل الإصلاح كانت هذه الاستدعاءات كلها `TS2304 Cannot find name` (المتغيّر نفسه غير معرَّف)، فـ`tsc` ما كانش بيوصل لفحص توافق الـmethods أصلًا. بعد الإصلاح ظهرت أعطال حقيقية مستخبية سابقًا، من فئتين: **(أ)** method غير موجودة بالاسم ده — `IoTService.getAssets` (بالجمع) مُستخدَمة، لكن الكائن الفعلي عنده `getAsset` (بالمفرد) بس (`TS2551 ... Did you mean 'getAsset'?`)؛ **(ب)** عدم توافق أنواع بين الـmethod الحقيقية وتوقيع `MutationFunction` من `@tanstack/react-query` المتوقَّع (`TranslationService.xxx` بتقبل `headers?` كمعامل ثانٍ اختياري، النوع المتوقَّع مختلف تمامًا). **صفر إصلاح — خارج نطاق #43 صراحة.** راجع `PROGRESS_LOG.md` (`iot-translation-service-method-mismatches-post-backlog43-fix`) للتفصيل الكامل. |
| 49 | `finance-transaction-idempotency-key-lookup-multiple-results-bug` [أُضيف 2026-08-30، بعد تجميد هذا الملف — اكتُشف أثناء التحقق الحي لبند #29، لكنه **باج منفصل تمامًا وكامن في `transfer()` من قبل #29 بكتير**، مش نتيجة لتعديلاتها] | 🟢 محلي — **✅ مُصلَح ومُتحقَّق منه حيًا [2026-08-30]** | 1 دالة مشتركة (`finance/repository.py`، `TransactionRepository.get_by_idempotency_key`) لكن تؤثر على أي كولر لها بمعاملة بطرفين (`transfer()`, `settle_held_funds()` الجديدة من #29) | `get_by_idempotency_key` (`finance/repository.py:97-109` قبل الإصلاح) كانت تعمل `.join(User, or_(User.id==sender_id, User.id==receiver_id))` — لو صف `Transaction` واحد عنده **كلاهما** `sender_id` و`receiver_id` غير فارغين (أي معاملة بطرفين حقيقيين)، الـJOIN يتطابق **مرتين** لنفس الصف → `scalar_one_or_none()` يرفع `MultipleResultsFound` (500) **بدل ما يرجع نفس المعاملة القديمة بأمان** — عكس الهدف الكامل من `idempotency_key`. كان كامنًا وغير مُلاحَظ لأن أي كولر سابق لـ`transfer()` على الأرجح ما عملش retry حقيقي فعلي بنفس المفتاح في اختبار حي؛ أول استدعاء حقيقي بيعمل retry فعلي على معاملة بطرفين (قرار #29: `idempotency_key` ثابت مُشتق من `auction_id` بدل `uuid4()` عشوائي لـ`settle_held_funds`) هو اللي فجّره لأول مرة. **الإصلاح المُطبَّق:** استبدال الـJOIN بـ`subquery` (`sender_id.in_(select(User.id)...)` أو `receiver_id.in_(...)`) — نفس شرط "الطرف بينتمي للتينانت" بدون تكرار الصف (`idempotency_key` عنده أصلًا `UNIQUE` index جزئي، `models.py:52`). صفر تغيير في السلوك المقصود — إصلاح تنفيذي بحت. **تحقق حي مستقل مخصَّص** (`tests/test_tenders_auctions_finance_hold_release_settle.py::test_transfer_idempotency_key_retry_returns_same_transaction`، منفصل عن اختبارات #29): استدعاء `transfer()` حقيقي مرتين بنفس `idempotency_key` على معاملة بطرفين حقيقيين — رجعت نفس المعاملة (`tx1.id == tx2.id`)، صفر خصم/استلام مزدوج، صف `transactions` واحد فقط. راجع `.claude/reports/finance-29-hold-funds-session-log.md` §12-13 و`PROGRESS_LOG.md`. |

**ملاحظة [2026-08-26، جلسة `batch5-audit-security-iot-translation-zamakana-admin-privacy-automation`]:** البنود #43-#45 أُضيفت بنفس الاستثناء المعتاد، بتوجيه صريح من المستخدم. #44 (`privacy.is_privacy_officer`) **اتصلح فعليًا في نفس الجلسة** (مع إصلاحات IDOR أخرى — `zamakana` الـ8 GET endpoints + create endpoints + `add_scenario_feedback`، `automation.create_workflow` — راجع تقرير الجلسة للتفاصيل الكاملة والتحقق الحي). #43/#45 توثيق فقط، صفر إصلاح.

**ملاحظة [2026-08-26، جلسة `batch4-audit-security-realestate-insurance-health` — تنفيذ IDOR البنود 1-4]:** البنود #40-#42 أُضيفت بنفس الاستثناء المعتاد (توثيق فقط أثناء جلسة أمنية، بتوجيه صريح من المستخدم). **صفر تنفيذ إصلاح لأي من الثلاثة — تحديدًا #42 ممنوع اللمس بلا نقاش منفصل بتوجيه صريح.** الإصلاحات الأمنية الفعلية المنفَّذة في نفس هذه الجلسة (`realestate.revalue_land`/`get_development`/`list_units_for_sale`، `insurance.get_policy`/`list_policies`، `health.get_facility`/`list_facilities` — نمط `get_current_tenant`/هيدر → `current_user.tenant_id` القياسي) **ليست بنود constructor-mismatch أصلًا** (فئة IDOR منفصلة تمامًا، موثَّقة في `critical-finding-xtenant-systemic.md`)، فلا تُدرَج في هذا الملف — راجع `batch4-audit-security-realestate-insurance-health.md` §4 للتفاصيل الكاملة والتحقق الحي.

**ملاحظة [2026-08-25، جلسة `batch3-audit-security-transport-logistics-manufacturing-tourism-sports`]:** البنود #34 إلى #39 أُضيفت بنفس الاستثناء، بتوجيه صريح من المستخدم. **خلافًا لكل البنود السابقة من هذه الفئة (توثيق فقط)** — #34/#35/#36 (منسوبة لـ`manufacturing`) **اتصلحت فعليًا في نفس الجلسة** (جنبًا إلى جنب مع إصلاح IDOR الأساسي، بتوجيه صريح: "مع إصلاح entity_id/الهاردكود/الـcommit المفقود سوا بما إنهم مرتبطين") — مؤكَّدة حيًا بعد الإصلاح، راجع تقرير الجلسة §6 للتفاصيل الكاملة. #37 (تحديث لبند معروف مسبقًا من `technical-pattern-sweep-session-log.md`، لم يكن له رقم رسمي في هذا الملف من قبل)، #38، و#39 **لسه بانتظار تنفيذ منفصل** (#38/#39 بقرار صريح: يُتركان لجلسة لاحقة).

**ملاحظة [2026-08-24]:** البندان #19 و#20 أُضيفا بعد تجميد هذا الملف (2026-08-17) بتوجيه صريح من المستخدم في جلستي `projects-idor-fix`/`academy-idor-fix`، خلافًا لملاحظة السطر 5 (الملف تصنيف فقط، لا يُضاف له بند جديد عادةً) — المصدر الكامل والتفاصيل في التقارير المذكورة مباشرة، هنا فقط للتصنيف السريع.

**ملاحظة [2026-08-25]:** البنود #21 إلى #26 أُضيفت بنفس الاستثناء، بتوجيه صريح من المستخدم في جلسة `batch1-audit-security-employment-invitations-communications-invoicing`. صفر تنفيذ إصلاح لأي منها بعد — تصنيف/توثيق فقط. **🔴 أولوية خاصة: #24 (`create_invitation` معطوبة بالكامل) أعلى أولوية من أغلب بنود المجموعة أ التالية** — مش خلل حافة، دي الوظيفة الأساسية لدومين كامل غير قابلة للاستخدام إطلاقًا حاليًا (صفر دعوة واحدة ممكن تُنشأ فعليًا عبر الـAPI، بغض النظر عن أي إصلاح IDOR).

**ملاحظة [2026-08-25، جلسة `batch2-audit-security-...`]:** البنود #27 إلى #31 أُضيفت بنفس الاستثناء، بتوجيه صريح من المستخدم بعد جلسة `batch2-audit-security-service-marketplace-tenders-auctions-arbitration-syndicates`. صفر تنفيذ إصلاح لأي منها بعد — تصنيف/توثيق فقط (باستثناء البند الأمني المنفصل `service_marketplace.webhook` سر ثابت، اللي اتصلح فورًا في نفس الجلسة ولا يُضاف هنا لأنه مش نمط باج متكرر، بل تصلب أمني منفَّذ مباشرة).

**✅ تحديث دفعي [2026-08-26]:** #2 (موضعان امتداد فقط)، #12، #17، #18 (تحفظ: بلا تحقق حي كامل)، #19، #20 اتعلَّموا ✅ (أو 🟡 لحالة #18) فوق، بناءً على commit `55032eb` (2026-08-25، جلسة `constructor-mismatch-backlog-cleanup`) — كل علامة مصحوبة باقتباس حي مباشر من `.claude/reports/constructor-mismatch-backlog-cleanup-session-log.md`، مش اعتمادًا على رسالة الـcommit وحدها.

**✅ تحديث دفعي ثانٍ [2026-08-27]:** #1، #5، #6، #7، #8، #9، #11 (بشقيه #11a/#11b)، #14، #15 اتعلَّموا ✅ فوق كمان — دي بنود قديمة (مُصلَحة فعليًا بين 2026-08-18 و2026-08-25 حسب `PROGRESS_LOG.md`) لكنها ماكانتش معلَّمة في الملف الموحَّد من الأساس، نفس فجوة `55032eb` بالظبط بس أقدم بشهر تقريبًا (راجع `PROGRESS_LOG.md` — ملاحظة process-gap مُلحَقة بإدخال جلسة `backend-fixes-post-url-prefix-audit`). كل علامة مصحوبة باقتباس حي مباشر من تقرير الجلسة الأصلي لكل بند (مش من `PROGRESS_LOG.md` كوسيط). **#28** (`saas-service-catalog-missing-entries`) اتعلَّم ✅ كمان بعد إصلاح فعلي جديد [2026-08-27] — راجع صفه في الجدول (فيه اكتشاف جانبي: `transport`/`tourism_sports`/`social` بنفس المشكلة، غير مُصلَحين، خارج نطاق البند الأصلي).

**تحذير مهم لسه قائم:** أقسام "🔴 المجموعة أ" و"🟢 المجموعة ب" و"الخلاصة العملية" أسفل هذا الملف **لسه بتعكس العدّ القديم** (قبل كل هذه التحديثات) — لم تُعاد كتابتها عمدًا لتفادي مخاطرة حسابية أثناء تنفيذ متزامن، فأي عدّ إجمالي هناك يُعتبر تاريخيًا/غير محدَّث لحد ما تُحدَّث دفعة واحدة في نهاية الجلسة الحالية. الجدول الشامل الحالي للبنود المتبقية فعليًا غير المُصلَحة موجود في تقرير منفصل: `.claude/reports/constructor-mismatch-backlog-remaining-approval-table.md`.

---

## 🔴 بند منتشر مستقل — #23 `bleach-clean-none-crash` (13 دومين، توثيق موسَّع، **بلا إصلاح الآن**)

**مفصول عمدًا عن #24-#26** (بتوجيه صريح من المستخدم) — دول محليون لـ`invitations` وحدها، بينما #23 نمط تقني عابر لـ13 دومين مستقلة، **نفس فئة `duplicate-kwarg-audit` (#2)** من حيث الشكل: سبب جذري واحد (`.get(key, default)` بترجّع الـ`default` لو الـ`key` غايب بس، **مش لو قيمته `None` صراحة**) منسوخ حرفيًا في كل موضع، بلا دالة/كلاس مشترك واحد يمكن إصلاحه مركزيًا (كل نداء `bleach.clean` مباشر في كود الدومين نفسه).

**السبب الجذري بالتفصيل:** أي حقل `Optional[str]` في Pydantic schema بيتحط `None` صراحة في `data.model_dump()` لو العميل ما بعتوش (الـkey **موجود** بالـdict بقيمة `None`، مش غايب) — فـ`data.get(field, "")` **بيرجّع `None`، مش `""`** (لأن `.get()` بيرجّع الـdefault بس لو الـkey غايب تمامًا). `bleach.clean(None, tags=[], strip=True)` بيرمي `TypeError: argument cannot be of 'NoneType' type` فورًا.

**منهجية التأكيد:** `grep -rn "bleach.clean(data.get(" app/domains/*/service.py | grep -v "if data.get"` (يستبعد المواضع المحمية فعليًا بحارس `if data.get(field) else None` على نفس السطر — هذه المواضع **آمنة**، مش جزء من العدّ). **تصحيح:** العدّ الأول (89) كان خطأ حسابي من grep متعدد الملفات بلا تجميع صحيح — **العدد الصحيح المُعاد حسابه واستقلاليًا لكل ملف: 52 موضع غير محمي عبر 13 دومين.**

| الدومين | عدد المواضع غير المحمية | مؤكَّد حيًا؟ |
|---|---|---|
| `invitations` | 8 | ✅ (`create_invitation`, `create_campaign` — كراش فعلي حي، جلسة `batch1-audit-security-...`) |
| `zamakana` | 8 | ⚪ grep فقط |
| `manufacturing` | 6 | ⚪ grep فقط |
| `tourism_sports` | 5 | ⚪ grep فقط |
| `command` | 4 | ⚪ grep فقط |
| `logistics` | 4 | ⚪ grep فقط |
| `tenders_auctions` | 4 | ⚪ grep فقط |
| `transport` | 4 | ⚪ grep فقط |
| `social` | 3 | ⚪ grep فقط |
| `ai_agents` | 2 | ⚪ grep فقط |
| `employment` | 2 | ⚪ grep فقط |
| `agritech` | 1 | ⚪ grep فقط |
| `realestate` | 1 | ⚪ grep فقط |
| **الإجمالي** | **52** | 1 دومين مؤكَّد حيًا (`invitations`)، 12 مطابقة بالـgrep بس |

**دومينات فُحصت واستُبعدت** (بتستخدم `bleach.clean` بنمط مختلف، غير عرضة لنفس الباج): `insurance` (وصول مباشر `data["key"]`، عطل مختلف — `KeyError` مش `TypeError`)، `arbitration_syndicates` (نفس الشيء)، `projects` (نداء مباشر بلا `.get()`).

**الإصلاح المقترَح (لم يُنفَّذ):** `bleach.clean(data.get(field) or "", ...)` بدل `.get(field, "")` في كل موضع — تعديل ميكانيكي بسيط لكل الـ52 موضع، أو wrapper مركزي `safe_clean(value)` يُستبدَل بيه كل نداء. **أولوية وظيفية عالية** (يمنع إنشاء موارد أساسية، مش قراءة/تعديل هامشي) — لكن يحتاج **جلسة `grep`/تحقق حي منفصلة لباقي الـ12 دومين** قبل أي إصلاح مركزي فعلي (نفس منهجية #2/#7/#8 وقت اكتشافها لأول مرة).

---

## 🔴 بند منتشر مستقل — #31 `frontend-service-url-prefix-mismatch` (21 من 30 دومين فرونت إند، توثيق موسَّع، **بلا إصلاح الآن**)

**اكتُشف أثناء تحقيق §7#15 من `batch2-audit-security-...` (كان محصورًا فيه بحث لـ`service_marketplace`/`tenders_auctions` فقط) — بتوجيه المستخدم اتوسّع لـ`grep` شامل عبر كل الـ32 ملف `services/*.ts`.** نفس فئة `bleach-clean-none-crash` (#23) من حيث الشكل: سبب جذري واحد **متكرر بنفس الآلية** (توليد المسار من دمج تسمية `main.py`/`routers_config` غير الموثوقة مع تسمية الراوتر الداخلي)، لكن بلا دالة/كلاس مشترك واحد — كل ملف `services/*.ts` مكتوب مستقلًا (على الأرجح مولَّد آليًا بنفس الأداة/القالب الخاطئ).

**السبب الجذري بالتفصيل:** `apiClient` (axios) بـ`baseURL = NEXT_PUBLIC_API_URL` (افتراضيًا `http://localhost:8000/api`، مؤكَّد من `.env.local`). المسار الحقيقي المسجَّل فعليًا لكل دومين = `/api` + `prefix` الداخلي لكائن `APIRouter` في `router.py` نفسه فقط (`main.py:300-308`: `include_router(router_obj, prefix="/api", tags=tags_list)` — **صفر استخدام** لحقل `prefix_path` الثاني في تركيبة `routers_config` رغم وجوده، وهو حقل ميت غير مُستخدَم في التسجيل الفعلي إطلاقًا). لكن أغلب ملفات `services/*.ts` تبدو مولَّدة بافتراض إن هذا الحقل الميت **هو المسار الفعلي**، فبتبني الرابط كـ`{prefix_path}{router.prefix}/...` — لما `prefix_path` يساوي `router.prefix` حرفيًا (الحالة الأشيع، 19 دومين)، الناتج بادئة مضاعَفة (`/employment/employment/jobs`)؛ ولما يختلفا (`arbitration_syndicates`: `/arbitration` مقابل `/arbitration-syndicates`)، الناتج مسار غير موجود إطلاقًا؛ و`tenders_auctions` حالة خاصة أسوأ — يبدو نسخ-لصق غير مكتمل من `social.ts` (يستخدم فعليًا segment `"social"` بدل `"tenders-auctions"`).

**منهجية التأكيد:** قراءة أول 3 روابط `apiClient.(get|post|put|delete|patch)` في كل ملف من الـ32 (`grep -oE` على الأنماط الحرفية + فحص القوالب النسبية بلا `/` بادئة)، مقارَنة بـ`APIRouter(prefix=...)` الفعلي في `router.py` المطابق (`grep` مباشر عبر `app/domains/*/router.py`) + `main.py:266-297` (تركيبة `routers_config`). **تحقق حي مباشر واحد إضافي** (خارج ما فعلناه للـ`service_marketplace` في `batch2-audit-security-...` §8) لتأكيد إن الفرضية تنطبق فعليًا لا نظريًا فقط: `GET /api/marketplace/services` (المسار الحقيقي) → `200`، بينما `GET /api/marketplace/marketplace/services` (المسار اللي يستخدمه `services/marketplace.ts` فعليًا بعد `baseURL`) → `404` — **مباشرة من سيرفر uvicorn حقيقي، بلا أي طبقة nginx/proxy/rewrite موجودة في المشروع** (تأكيد: `docker-compose.yml` بلا أي خدمة nginx/proxy، `eppne-web` بلا `next.config.js` rewrites، بلا `app/api/*` وسيط).

| النمط | عدد الدومينات | الدومينات |
|---|---|---|
| **بادئة مضاعَفة** (`{domain}/{domain}/...` — `prefix_path == router.prefix` حرفيًا) | **19** | `affiliate`, `command`, `commerce`, `communications`❌(راجع تصحيح تحت)، `employment`, `health`, `insurance`, `invitations`, `iot`, `logistics`, `manufacturing`, `service_marketplace`(`marketplace.ts`)، `privacy`, `realestate`, `saas`, `social`, `tourism_sports`, `translation`, `transport`, `zamakana` |
| **مسار مغلوط تمامًا** (`prefix_path != router.prefix`، أو نسخ-لصق من دومين آخر) | **2** | `arbitration_syndicates` (`/arbitration/arbitration-syndicates/...` — دمج الاثنين معًا، مسار غير موجود إطلاقًا)، `tenders_auctions` (`/tenders/social/...` — segment "social" منسوخ من دومين تاني بالخطأ) |
| **صحيح (بادئة واحدة، مطابقة للمسار الحقيقي فعليًا)** | **9** | `academy`, `ai_agents`(`ai-agents.service.ts`، يستخدم `/ai` الحقيقي مش `/ai-agents` من `routers_config`)، `identity`(`auth.service.ts`)، `automation`, `digital_twin`, `finance`, `projects`(مسارات نسبية بلا `/` بادئة، تُحل صح عبر `combineURLs` في axios)، `sovereign_entities`(`sovereign-entities.ts`، يستخدم `/sovereign-entities` الحقيقي مش `/sovereign` من `routers_config`) |
| **خارج النطاق** (بلا ملف `services/*.ts` مخصَّص، أو الدومين محذوف من الباك إند) | 3 | `ai_governance`, `invoicing`, `admin` (بلا ملف)، `agritech` (ملف موجود بنفس عيب البادئة المضاعَفة، لكن الراوتر الخلفي **محذوف بالكامل** من `main.py` — العطل هنا أعمق من مجرد مسار خاطئ، غير معدود ضمن الـ21) |

**تصحيح مهم أثناء إعادة التحقق لكتابة هذا الجدول:** `communications.service.ts` أول عيّنة أظهرت مسارات صحيحة (`/communications/devices/register`) — **هذا لا يعني الملف بالكامل سليم بالضرورة**؛ العدّ هنا اعتمد على عيّنة 3 روابط أولى لكل ملف (نفس منهجية `bleach-clean-none-crash` وقت اكتشافه لأول مرة) — **لم يُفحَص كل رابط في كل ملف بالكامل**، فالعدد (21/30) قابل للتعديل الطفيف لو جلسة `grep` مخصَّصة لاحقة فحصت كل سطر بدل عيّنة.

**الإصلاح المقترَح (لم يُنفَّذ):** تصحيح المسار الحرفي (أو القالب المولِّد له لو موجود) في كل ملف من الـ21 ليطابق `APIRouter(prefix=...)` الفعلي مرة واحدة فقط (حذف التكرار، أو تصحيح الاسم بالكامل لحالتي `arbitration_syndicates`/`tenders_auctions`) — تعديل ميكانيكي بسيط في **بيانات نصية فقط** (لا منطق)، لكن **لا يمكن دمجه مع #1 من `constructor-mismatch` نفسه** (فئة مختلفة تمامًا — لا علاقة بتوقيع دالة/kwarg). **يستحق جلسة `grep`+تصحيح مركزية مستقلة كاملة** تراجع كل سطر (مش عيّنة) في الـ32 ملف، **قبل أي جلسة إصلاح فرونت إند تانية** (لأن أي إصلاح TS2305 لأسماء استيراد — زي `service_marketplace`/`tenders_auctions`/`arbitration_syndicates` بالدفعة 2 — عديم الفائدة عمليًا لحد ما هذا البند يتصلح جنبه، الاثنان لازم يتصلحوا معًا في نفس الدومين عشان أي شاشة تشتغل فعليًا).

**✅ تحديث [2026-08-26/27]: البند اتصلح فعليًا** عبر commit `6b4703a` — راجع صف #31 في الجدول الموحَّد فوق للدليل الحي الكامل (تحقق طلب حقيقي واحد لكل ملف ضد سيرفر حي، بعد التصحيح، `200`/`500`/`403` بدل `404`). التحفظ الوحيد المتبقي: التصحيح اعتمد على **عيّنة أول 3 روابط لكل ملف** وقت الجرد الأولي (مش كل سطر) — احتمال ضئيل لموضع إضافي غير مكتشَف داخل نفس الـ21 ملف يفضل قائم نظريًا، لكن التحقق الحي (طلب فعلي واحد على الأقل لكل ملف بعد التصحيح) يثبت إن المسار الأساسي الموصوف اتصلح فعلًا لكل الـ21.

---

## 🔴 المجموعة أ — تحتاج جلسة تصحيح مركزي واحدة (13 بند: #1, #2, #7, #8, #9, #10, #11, #12, #14, #15, #16, #23, #31)

**✅ الحالة الفعلية النهائية [تحديث 2026-08-29]** — العدّ التاريخي (13 بند مفتوح) بقى قديم. الحالة
الحقيقية دلوقتي: **8 مُغلَقة بالكامل** (#1، #7، #8، #9، #11، #12، #14، #31)، **4 جزئيًا** (#2 —
موضعان امتداد بس مُصلَحين، النطاق الأوسع 4+ دومينات غير مؤكَّد بالكامل؛ #10 — محجوب بقرار تصميمي
لسه مفتوح (`identity-user-referred-by-field-missing`)؛ #15 — الآلية الأساسية مُصلَحة، بج شقيق منفصل
(`ai-governance-usage-log-idempotency-wrong-arity`) لسه مفتوح؛ #16 — 17 من 19 موضع، موضعان محجوبان
بتعارض `commit()`/`begin_nested()` لسه مفتوحين)، **بند واحد لسه مفتوح بالكامل** (#23 —
`bleach-clean-none-crash`، 12 من 13 دومين لسه غير مُصلَحين، راجع القسم المخصَّص أعلاه). راجع الجدول
الموحَّد فوق لكل بند لأدلة التحقق الحي التفصيلية.

هذه البنود **كلها من نفس الشكل البنيوي**: كلاس/دالة مشتركة واحدة (`UserRepository`, `RedisClientWrapper`, `SaaSControlService`, `AffiliateService`, `InvoicingService`, `audit_log()`, `AIGovernanceService`, `AIAgentsService`) تغيَّر توقيعها أو ناقصة method — ونفس الخطأ **منسوخ حرفيًا** في كل دومين استخدمها. إصلاح دومين واحد بمفرده **لا يحل** الباقي؛ الإصلاح الصحيح هو تعديل الكلاس/الدالة نفسها (أو `grep` شامل + تعديل كل الاستدعاءات دفعة واحدة) في **جلسة واحدة مخصَّصة لكل بند**.

### ترتيب الأولوية المقترَح داخل هذه المجموعة

0. **#24 (`invitations-create-invitation-fully-broken`, محلي لـ`invitations`)** — 🔴🔴 **أولوية تسبق حتى #11/#9 من ناحية الأثر الوظيفي (لا تسبقهما من ناحية الخطورة الأمنية/المالية)**: وظيفة "إنشاء دعوة" — جوهر دومين `invitations` — **معطوبة 100%، صفر استخدام ناجح ممكن حاليًا عبر الـAPI**، بغض النظر عن أي إصلاح IDOR. عطلها الأول (`bleach.clean` على `None`) مظهر محلي من #23 (راجع القسم المخصَّص أعلاه) — الفرق إن #11/#9 خطورتهما **أمنية/مالية**، و#24 خطورته **وظيفية بحتة** (منع كامل لميزة، صفر تسريب/فقدان بيانات).
0.5. **#23 (`bleach-clean-none-crash`، بند منتشر مستقل — راجع القسم المخصَّص أعلاه، مفصول عمدًا عن #24)** — نفس مرتبة #12/#15/#16 تقريبًا من ناحية الجاهزية (نمط مؤكَّد جزئيًا بالتنفيذ الحي، يحتاج تحقق حي لباقي الـ12 دومين قبل جلسة الإصلاح المركزي الفعلية) — **لا يُصلَح كجزء تلقائي من إصلاح #24** (نفس المنطق: إصلاح موضع `invitations` بمفرده لا يحل باقي الـ12 دومين).
1. **#11 (`realestate-invoicing-savepoint-conflict` + امتداد `invitations`)** — 🔴🔴 **أعلى أولوية مطلقة في كل الـBacklog**. بيانات هوية حقيقية يتيمة على القرص (`users id=52`, `p_ctor_inv_newuser@eppne.com`). جلسة `invitations-user-registration-savepoint-leak` **مقررة بالفعل كالجلسة التالية مباشرة** بعد الانتهاء من مراجعة دفعة `constructor-mismatch` الحالية — راجع `.claude/reports/CRITICAL-invitations-accept-orphaned-user-no-wallet.md`.
2. **#9 (`saas-control-service-missing-methods`)** — 🔴🔴 **محظور إصلاحه بمعزل عن #11**: `get_active_subscription` المفقودة هي نفسها اللي بتحجب (بالصدفة، مش بتصميم آمن) ثغرة `invitations` حاليًا. أي إصلاح لـ#9 **قبل** إغلاق #11 هيفتح الثغرة فورًا في كل دومين بيستخدم نمط `get_active_subscription` (`digital_twin`, `employment`، وربما أكتر).
3. **#14 (`audit-log-wrong-kwargs`)** — الأوسع انتشارًا رقميًا في كل الـBacklog (عشرات المواضع + داخل `InvoicingService` نفسها) — بمجرد إغلاق #11/#9، هذا البند يستاهل الأولوية التالية لأنه بيمنع **كل** عملية `create_invoice`/`audit_log` ناجحة ظاهريًا من الاكتمال فعليًا عبر المشروع كله تقريبًا.
4. **#1 (`user-repository-get-by-id-audit`)** — فشل مالي صامت مؤكَّد حيًا (عمولات affiliate بتضيع بلا أي إشارة خطأ) — أولوية عالية لأنه صامت.
5. **#10 (`affiliate-service-missing-methods`)** — مرتبط مباشرة بـ#1 (نفس الدالة المساعدة `_register_affiliate_commission` في كل دومين تقريبًا) — يستحسن إصلاحهما في نفس الجلسة أو جلستين متتاليتين.
6. **#12، #15، #16** — نفس العائلة (استدعاءات بمعامل `tenant_id` زيادة بعد نقله للـconstructor) — **🔴 تصحيح [2026-08-17]، بعد تدقيق تقني كامل لكل موضع فعلي: التجميع في جلسة واحدة موحّدة كان افتراضًا غير دقيق.** #12 فعلاً متطابق 100% (6/6 مواضع بسيطة). لكن #15 (7 من 13 موضع = 54%) و#16 (13 من 18 موضع = 72%) فيهم نسبة كبيرة من الحالات "المركّبة" (زي `arbitration_syndicates`) — مش استثناء نادر زي ما كان موثَّق، دي الأغلبية فعليًا. التفاصيل الكاملة + جدول مقارَنة موضع بموضع في `.claude/reports/constructor-mismatch-backlog-12-15-16-homogeneity-audit.md`. **القرار المُحدَّث: #12 جلسة ميكانيكية مستقلة، #15+#16 يُقسَّموا لـ3 جلسات فرعية (ميكانيكي / مركّب-سهل / مركّب-صعب) بدل جلسة واحدة.**
7. **#8، #7، #2** — أولوية أقل نسبيًا (أعداد مواضع أصغر، بعضها محمي بـ`try/except` فبيفشل بهدوء) — تُجدوَل بعد ما سبق.

---

## 🟢 المجموعة ب — محلية لدومين واحد (16 بند: #5, #6, #17, #18, #19, #20, #21, #22, #25, #26, #27, #28, #29, #33, #46, #47 — + #24 مُدرَج تصنيفيًا هنا لكن بأولوية استثنائية أعلى، راجع بند 0 بالمجموعة أ)

**✅ الحالة الفعلية النهائية [تحديث 2026-08-29، جلسة `invoicing-21-metadata-collision`]:** **12 مُغلَقة
بالكامل** (#5، #6، #17، #19، #20، #21، #22، #25، #28، #33، #46، #47)، **2 جزئيًا** (#18 — الكود
مُصلَح لكن بلا تحقق حي كامل لغياب جداول `transport`؛ #27 — `list_user_cases` بس)، **2 لسه مفتوحة
بالكامل، صفر لمس** (#26 — معالجة أخطاء ناقصة؛ #29 — `hold_funds`/`release_held_funds` غير موجودتين
على `FinanceService`). **#24 لسه مفتوح بالكامل** (كلا عطليه — `bleach.clean(None)` جزء من #23
المفتوح، و`_assign_ai_agent`/`PaginatedResponse` — بلا لمس).

هذه البنود **لا تشترك في سبب جذري مع أي بند تاني** — كل واحد مرتبط بمنطق/schema/قرار خاص بدومين واحد فقط. لا تحتاج جلسة عابرة للدومينات؛ إصلاح كل واحد منفصل تمامًا عن الباقي.

- **#5 (`sovereign_entities`)** — قرار منتجي (هل الـ4 endpoints دي المفروض تفضل بلا مصادقة ولا لأ) — مش باج تقني.
- **#6 (`commerce.visa_webhook`)** — مراجعة أمنية خاصة ببوابة دفع واحدة (توقيع، تكرار، مصدر tenant_id) — بطبيعتها محلية.
- **#17 (`arbitration-case-model-idempotency-key-mismatch`)** — عمود مفقود في موديل SQLAlchemy واحد (`ArbitrationCase`) — إصلاحه هو إضافة العمود، صفر علاقة بأي دومين تاني.
- **#18 (`finance-service-create-invoice-does-not-exist`)** — استدعاء خاطئ لـmethod غير موجودة، محصور في `transport` فقط حاليًا. **تحفظ:** لم يُنفَّذ `grep` شامل لتأكيد عدم وجود نفس الخطأ (`self.finance.create_invoice`) في دومين تاني — لو حبيت تأكيد 100%، ده أقرب لبند #13 (يحتاج جرد سريع أولاً) أكتر من كونه محلي مؤكَّد نهائيًا.
- **#19 (`projects-schema-model-field-mismatch`)** [أُضيف 2026-08-24] — 3 حقول/جداول Pydantic schema بلا أعمدة SQLAlchemy مقابلة، محصورة في دومين `projects` وحده (`Project`, `Contribution`, `ProjectUpdate`) — صفر سبب جذري مشترك مع أي دومين تاني.
- **#20 (`academy-create-bootcamp-instructor-id-mismatch`)** [أُضيف 2026-08-24] — `instructor_id` بيتمرر لـ`Bootcamp(**kwargs)` لكن العمود غير موجود في الموديل الفعلي، محصور في `academy.create_bootcamp` وحدها — صفر سبب جذري مشترك مع أي دومين تاني.
- **#21 (`invoicing-invoice-response-metadata-collision`)** [أُضيف 2026-08-25] — تصادم اسم حقل `metadata` مع `SQLAlchemy.Base.metadata`، محصور في `InvoiceResponse`/`Invoice` بدومين `invoicing` وحده — صفر سبب جذري مشترك مع أي دومين تاني. **✅ مُغلَق بالفعل [تاريخ اكتشاف: 2026-08-29، مصدر الإصلاح الفعلي: commit `93e68ac` بتاريخ 2026-08-25]** — الإصلاح حصل بالفعل في نفس يوم إضافة هذا البند للـbacklog، كإصلاح جانبي مذكور صراحة داخل commit معنون لبند أمني مختلف تمامًا (`fix(invoicing): close create_invoice mass-assignment, fix masked metadata bug`) — لهذا فات تصنيف البند وقتها كـ"مفتوح". تحقق حي [2026-08-29] أكَّد صفر أثر للعطل حاليًا (5 فواتير حقيقية مُرِّرت عبر `InvoiceResponse.model_validate()` بلا استثناء). راجع `.claude/reports/invoicing-21-metadata-collision-session-log.md` للتفصيل الكامل.
- **#22 (`invitations-leads-route-ordering`)** [أُضيف 2026-08-25] — باج ترتيب تسجيل routes، محصور في `invitations/router.py` وحده (نفس الفئة الميكانيكية زي `sovereign_entities` سابقًا، لكن حالة مستقلة تمامًا) — صفر سبب جذري مشترك مع أي دومين تاني.
- **#24 (`invitations-create-invitation-fully-broken`)** [أُضيف 2026-08-25] — مُركَّب من عطلين (أحدهما مظهر محلي لـ#23 المركزي، والتاني `_assign_ai_agent` محلي بحت) — **🔴🔴 أولوية عالية استثنائية رغم كونه محليًا**: يمنع الوظيفة الأساسية للدومين بالكامل، راجع الملاحظة أعلى.
- **#25 (`invitations-legacy-invitation-rows-response-validation-error`)** [أُضيف 2026-08-25] — بيانات throwaway قديمة تالفة (NULL في أعمدة صارت إلزامية لاحقًا)، محصور في 3 صفوف بجدول `sovereign_invitations_v2` — صفر سبب جذري مشترك مع أي دومين تاني. **✅ مُصلَح [2026-08-26]** (schema بقى يتحمَّل `None`، راجع الصف في الجدول الموحَّد فوق).
- **#26 (`invitations-update-not-found-500`)** [أُضيف 2026-08-25] — معالجة أخطاء ناقصة في `update_lead`/`update_ticket` (500 بدل 404)، محصور في `invitations` (لم يُفحَص انتشاره) — صفر سبب جذري مشترك مؤكَّد مع أي دومين تاني حاليًا.
- **#27 (`arbitration-syndicates-repository-missing-methods`)** [أُضيف 2026-08-25، جلسة `batch2-audit-security-...`] — **✅ مُصلَح بالكامل [2026-08-29]** — 6/6 دوال (الخمسة الأصليون + `list_syndicate_elections` المُكتشَفة أثناء التحقيق)، كل واحدة مؤكَّدة حيًا بلا IDOR. راجع الصف في الجدول الموحَّد فوق وتقرير الجلسة الكامل `.claude/reports/arbitration-syndicates-repository-27-session-log.md`.
- **#46 (`insurance-policies-response-validation-terms-and-conditions-null`)** [أُضيف 2026-08-26، جلسة تحقق حي لبند #31] — **✅ مُصلَح [2026-08-26]** — `terms_and_conditions` بقى `Optional[Dict[str, Any]]`، يعكس واقع العمود القابل لـ`NULL` في DB، صفر سبب جذري مشترك مع أي دومين تاني.
- **#47 (`translation-redis-client-singleton-called-as-factory`)** [أُضيف 2026-08-26، جلسة تحقق حي لبند #31] — **✅ مُصلَح [2026-08-26]** — استيراد `redis_client` مباشرة بلا alias مضلِّل، صفر سبب جذري مشترك مع أي دومين تاني.
- **#28 (`saas-service-catalog-missing-entries`)** [أُضيف 2026-08-25، جلسة `batch2-audit-security-...`؛ **مُوسَّع 2026-08-26**] — بيانات seed ناقصة في `saas_service_catalog` لـ`tenders`/`auctions`/`service_marketplace`/`zamakana`، محصور بثلاثة دومينات حاليًا، وهي فجوة بيانات إعداد مش خطأ برمجي مشترك.
- **#29 (`finance-service-hold-funds-missing`)** [أُضيف 2026-08-25، جلسة `batch2-audit-security-...`] — `FinanceService.hold_funds`/`release_held_funds` غير موجودتين، محصور في `tenders_auctions` وحده حاليًا (لم يُفحَص انتشاره).
- **#33 (`tenders-auctions-naive-vs-aware-datetime`)** [أُضيف 2026-08-25، جلسة `batch2-audit-security-...`] — `datetime.utcnow()` naive مقارَن مباشرة ضد أعمدة `timezone=True` aware في `submit_bid`/`place_bid`، محصور في `tenders_auctions` وحده حاليًا (لم يُفحَص انتشاره).

---

## 🟡 المجموعة ج — غير مؤكَّدة الانتشار، تحتاج جرد قبل التصنيف النهائي (بندان: #13, #32)

**✅ الحالة الفعلية النهائية [تحديث 2026-08-29]:** الجرد خلص للاتنين. **#32 مُغلَق بالكامل**
(`grep` تأكيدي: صفر انتشار، موضع واحد بس، مُصلَح ومُتحقَّق منه حيًا). **#13 لسه مفتوح** — الجرد
الشامل (21 موضع `create_invoice(` عبر كل الدومينات) أكَّد **صفر انتشار كمان** (`service_marketplace`
هو الموضع الوحيد)، لكن الموضع نفسه **لم يُصلَح بعد** (تصحيح على الملاحظة القديمة "يُعالَج ضمن جلسة
#14" — #14 دالة مختلفة تمامًا `audit_log()`، لا علاقة لها بـkwarg `create_invoice` — راجع
`.claude/reports/constructor-mismatch-backlog-inventory-4-13-session.md`).

- **#13 (`invoicing-create-invoice-wrong-kwarg`)** — الجزء الخاص باسم الـkwarg (`tenant_id=` بدل `entity_id=`) مؤكَّد في `service_marketplace` بس لحد الآن، لكن التقرير نفسه ينص صراحة على إن `grep` شامل لباقي الـ12 دومين المستخدمة لـ`InvoicingService.create_invoice` **لم يُنفَّذ بعد**. **ملاحظة مهمة:** امتداده الأخطر (كراش `audit_log` **داخل** `create_invoice()` نفسها، بغض النظر عن صحة اسم الـkwarg الخارجي) **مؤكَّد بالفعل كمركزي 100%** ومُدمَج فعليًا تحت البند #14 — لذلك القرار العملي هو: **عالج #13 كجزء من جلسة #14 مباشرة**، ولا داعي لجلسة `grep` منفصلة قائمة بذاتها.
- **#32 (`arbitration-syndicates-nominate-candidate-wrong-kwarg`)** [أُضيف 2026-08-25] — `candidate_user_id` بدل `user_id` في نداء `ElectionCandidate(**kwargs)`، مؤكَّد في `arbitration_syndicates` وحده لحد الآن — لم يُفحَص انتشار نفس نمط تسمية الـkwarg في دومين تاني.
- **#43 (`frontend-service-export-import-name-mismatch`)** [أُضيف 2026-08-26، جلسة `batch5-audit-security-...`] — export/import mismatch في ملفات `services/*.ts` (2 مؤكَّدة: `realestate`, `iot`)، مستقل عن #31 — لم يُفحَص انتشاره عبر باقي ملفات الفرونت إند.
- **#44 (`permission-check-passed-id-instead-of-user-object`)** [أُضيف 2026-08-26، جلسة `batch5-audit-security-...`] — تمرير `int` (ID) بدل كائن `User` كامل لدالة صلاحيات، مؤكَّد في `privacy.is_privacy_officer` وحده لحد الآن (✅ مُصلَح) — لم يُفحَص انتشار نفس النمط في دومين تاني.
- **#45 (`global-unique-column-conflicts-with-tenant-scoped-query`)** [أُضيف 2026-08-26، جلسة `batch5-audit-security-...`] — عمود `unique=True` عالمي يتعارض مع فلترة tenant-scoped بالاستعلام، مؤكَّد في `translation.TranslationCache.text_hash` وحده لحد الآن — لم يُفحَص انتشار نفس النمط في دومين تاني.

---

## ⚪ بنود إدارية / ليست أنماط باج (3 بنود: #3, #4, #30)

**✅ الحالة الفعلية النهائية [تحديث 2026-08-29]:** لسه التلاتة مفتوحة/بلا إصلاح. **#4** اتشخَّصت
بدقة أكبر (جلسة جرد كودي 2026-08-29 — الموضعان `saas.process_auto_renewals`/`can_access_service`
مؤكَّدان كودًا بثقة عالية، نفس نمط `cancel_subscription` المُصلَح بالضبط، لكن بلا تحقق حي فعلي
وبلا إصلاح بعد — راجع `.claude/reports/constructor-mismatch-backlog-inventory-4-13-session.md`).
**#3** و**#30** بلا أي تحديث.

- **#3 (Phase 16 الأصلي)** — استكمال جلسة سابقة (`phase16-session-log.md`)، مش نمط باج مكتشف حديثًا.
- **#4 (`silent-write-regression` المتبقي)** — موضعان أخيران (`saas.process_auto_renewals` فرع `except`, `saas.can_access_service`) من جلسة `silent-write-regression-session-log.md` القائمة أصلًا وشبه مكتملة (89 موضع اتغطوا already) — إغلاق أخير، مش جلسة مركزية جديدة.
- **#30 (`tenders-auctions-router-no-read-endpoints`)** [أُضيف 2026-08-25، جلسة `batch2-audit-security-...`] — فجوة تصميم/تنفيذ ناقص (صفر `router` handler للقراءة رغم وجود الدوال في `repository.py`)، مش نمط constructor mismatch.

---

## الخلاصة العملية

| السؤال | الإجابة |
|---|---|
| كام بند يحتاج جلسة مركزية واحدة فعلية؟ | **13 بند** (#1, #2, #7, #8, #9, #10, #11, #12, #14, #15, #16, #23, #31 — بعضها ممكن يتجمّع في نفس الجلسة زي #12+#15+#16) |
| كام بند محلي فعلاً، صفر حاجة لجلسة عابرة؟ | **15 بند** (#5, #6, #17, #18, #19, #20, #21, #22, #24, #25, #26, #27, #28, #29, #33 — #24 محلي تصنيفيًا لكن بأولوية استثنائية أعلى، راجع بند 0 بالمجموعة أ) |
| كام بند يحتاج جرد سريع الأول قبل تحديد نوعه؟ | **بندان** (#13 — لكن عمليًا يُعالَج ضمن جلسة #14؛ #32 محلي لحد ما يُفحص انتشاره) |
| كام بند إداري بس؟ | **3 بنود** (#3, #4, #30) |
| إيه أعلى أولوية مطلقة دلوقتي (أمنية/مالية)؟ | **#11 ثم #9** (بالترتيب ده تحديدًا — عكس الترتيب ممنوع لأنه بيفتح ثغرة هوية حقيقية) |
| إيه أعلى أولوية وظيفية (منع كامل لميزة، بمعزل عن الأمان)؟ | **#24 ثم #23** — `invitations.create_invitation` معطوبة 100% حاليًا، صفر دعوة واحدة ممكن تُنشأ عبر الـAPI |

### ✅ تحديث الخلاصة العملية [2026-08-29] — الجدول فوق تاريخي، الجدول ده يعكس الحالة الفعلية الآن

| السؤال | الإجابة المُحدَّثة |
|---|---|
| كام بند من مجموعة "مركزي" (أ) لسه محتاج شغل فعلي؟ | **5 بنود** (#2 امتداد أوسع غير مؤكَّد، #10 محجوب بقرار تصميمي، #15 بج شقيق منفصل، #16 موضعان محجوبان، #23 مفتوح بالكامل) — **8 من الـ13 الأصليين مُغلَقين بالكامل** (#1، #7، #8، #9، #11، #12، #14، #31) |
| كام بند من مجموعة "محلي" (ب) لسه محتاج شغل فعلي؟ | **6 بنود** (#18 جزئي، #21، #26، #27 جزئي، #29، #24) — **11 من الـ16 الأصليين مُغلَقين بالكامل** |
| كام بند من مجموعة "غير مؤكَّد" (ج) لسه محتاج شغل فعلي؟ | **بند واحد بس** (#13 — الجرد خلص، صفر انتشار، لكن الموضع نفسه لسه غير مُصلَح) — **#32 مُغلَق بالكامل** |
| كام بند إداري لسه مفتوح؟ | **3** (#3، #4، #30) — بلا تغيير عددي، لكن #4 اتشخَّص بدقة أكبر |
| بنود جديدة اتصلحت النهاردة خارج الأقسام الثلاثة الأصلية؟ | **#37 (4 مواضع)، #38، #40، #43 (جزئي — 8 ملفات)، #45، #48 (جديد، توثيق فقط)** |
| **إجمالي البنود المُغلَقة بالكامل عبر كل الجلسة (اليوم + قديمًا)** | **27 بند/موضع مُغلَق بالكامل**: #1، #5، #6، #7، #8، #9، #11، #12، #14، #17، #19، #20، #22، #25، #28، #31، #32، #33، #37، #38، #40، #45، #46، #47 (24 بند مُرقَّم) + توسيع #28 (كود `tourism`) + `list_leads`/`stats`/`campaigns`/`tickets` (توسيع #22) + 8 ملفات #43 |
| **البنود المتبقية فعليًا (جزئيًا أو بالكامل)، للموافقة على أولوياتها لاحقًا** | #2، #3، #4، #10، #13، #15 (بج شقيق)، #16، #18، #21، #23، #24، #26، #27، #29، #30، #43 (`realestate` جزء منفصل)، #48 |
