# #27 (بقية) — قرارات مطلوبة قبل أي تنفيذ

**مرجع كامل (تحقيق + تحقق حي):**
`.claude/reports/arbitration-syndicates-repository-27-session-log.md`

**الحالة:** 🔄 صفر تنفيذ حتى الآن. اكتب إجابتك جوه كل بند تحت "**الرد:**" واحفظ الملف.

---

## جدول التصنيف النهائي

| # | الدالة | endpoint المتأثر | تصنيف العطل | IDOR محتمل لو أُصلح بسذاجة؟ | أولوية |
|---|---|---|---|---|---|
| 1 | `list_user_licenses` | `GET /licenses/me` | منطق أعمق — يحتاج كتابة فلترة `user_id`+`tenant_id` معًا من الصفر | ⚠️ متوسط | **الأعلى** (صمت خطير — `[]` بدل عطل واضح) |
| 2 | `list_candidates` | `GET /elections/{id}/candidates` | سطر واحد ظاهريًا، لكن `service` مالهاش تحقق ملكية `election_id` إطلاقًا | 🔴 عالي | عالية |
| 3 | `list_syndicate_elections` **(دالة سادسة إضافية، خارج الخمسة المذكورين أصلًا)** | `GET /syndicates/{id}/elections` | غير موجودة إطلاقًا — كتابة كاملة | 🔴 عالي | عالية |
| 4 | `get_election_votes` | لا يوجد مباشر (retry idempotency فقط) | سطر واحد نسبيًا | ⚠️ منخفض-متوسط | منخفضة |
| 5 | `get_syndicate_memberships` | لا يوجد مباشر (retry idempotency فقط) | سطر واحد نسبيًا، لكن النداء الحالي بلا `tenant_id` إطلاقًا | ⚠️ منخفض-متوسط | منخفضة |
| 6 | `get_cases_by_claimant` | لا شيء — كود ميت غير قابل للوصول حاليًا | تنظيف فقط | لا ينطبق | الأقل |

---

## القرارات المطلوبة

### 1. ترتيب الأولوية
البدء بـ#1 (`list_user_licenses`) ثم #2/#3 (IDOR كامن)، ثم #4/#5، ثم #6 — ولا ترتيب مختلف؟

**الرد:**

---

### 2. نطاق البند — ضم #3 رسميًا؟
`list_syndicate_elections` مش من الخمسة المذكورين في تعليمات الجلسة، لكنها السبب الفعلي الوحيد
لعطل `GET /syndicates/{id}/elections`. موافق على ضمّها لنطاق تنفيذ #27، ولا تُفتح كبند منفصل؟

**الرد:**

---

### 3. مصير الكود الميت (#6)
حذف الفرع الميت (`get_cases_by_claimant` + شرط `hasattr` في `service.py`) بالكامل، ولا حذف
الدالة الميتة فقط وترك تبسيط `service.py` لجلسة تانية؟

**الرد:**

---

### 4. تصحيح مرجعي منفصل (غير مرتبط بالتنفيذ)
بورت DB في `throwaway-test-users.md` (`5433`) غلط/قديم — الصحيح المؤكَّد اليوم `5435`
(الحاوية `5433` مفيهاش حتى قاعدة بيانات `eppne_v2`). موافق على تحديث الملف المرجعي؟

**الرد:**

---

**بعد ما تحفظ إجاباتك هنا، أكمل التنفيذ حسب توجيهك.**
